# JOSEONLIKE — Tasks (Full Rebuild)

One feature per session, per [CLAUDE.md](CLAUDE.md). Phases: [ROADMAP.md](ROADMAP.md).
Tick a box only after commit **and** push succeeded.

2026-08-14: 오너 지시로 전면 리빌드. 구 백로그(R-시리즈)와 done log는
git 히스토리(`63b50c8` 이전)에만 남긴다. 구 코드/에셋 참조 금지.

---

## N0 — 리셋 & 부트 — DONE

- [x] **N0-1 리포 리셋** — scenes/ scripts/ tests/ tools/ asset/ 및 구
      게임 파일 제거 (Neo둥근모 ttf + ASSET_LICENSES.md 항목은 유지·이동).
      data/, 문서, example/, new_asset/ 유지. project.godot 정리.
- [x] **N0-2 최소 부트** — NIGHT 배경 + 픽셀 폰트 "조선라이크" 타이틀
      스텁이 에러 없이 뜬다. 헤드리스 테스트 러너 골격이 PASS를 출력한다.

## N1 — 타이틀 — CURRENT

- [x] N1-1 타이틀 레이아웃 (플레이스홀더 아트, DESIGN.md §4 배치)
- [x] N1-2 codex AC-2 아트 통합 (배경 레이어 + 현판 로고)
- [x] N1-3 설정 팝업 (음량/언어, 자동 저장) — N5-2에서 구현

## N2 — 수행자 선택

- [x] N2-1 characters.json 재검증 + 카드 리스트 화면 (도사 1 + 잠금 카드)
- [x] N2-2 선택 상태 저장/복원, 선택됨 배지 — N2-1에서 함께 구현
      (SaveService persist, 런 시작 시 선택 캐릭터 스탯/무기 사용)
- [ ] N2-3 codex AC-1 초상 통합

## N3 — 전투 수직 슬라이스 (세션 단위로 분해)

- [x] N3-1 스테이지 씬 + 카메라 + 가상 조이스틱 이동
- [x] N3-2 도사 인월드 스프라이트 (AC-1, 2방향 + 4프레임 걷기)
- [x] N3-3 자동 공격 무기 1종 (부적 투척)
- [x] N3-4 몬스터 스폰/추적/접촉 피해/사망
- [x] N3-5 XP 드랍 → 자석 픽업 → 레벨업
- [x] N3-6 파워 업 팝업 (행 카드 문법, 실수치 설명)
- [x] N3-7 전투 HUD (타이머/XP바/카운터, DESIGN.md §3)
- [x] N3-8 피격 피드백 (데미지 숫자는 N3-3에서 선반영)
- [x] N3-9 절차 생성 스테이지 필드 — 대나무숲 프롭 (충돌형 솔리드 + 데코,
      매 런 새 랜덤 배치, data/props.json 카탈로그)
- [x] N3-10 대나무숲 아트 통합 (AC-4) — 실제 타일/프롭 텍스처 렌더링,
      시드 기반 재현 가능한 타일 변형, 하단부 기준 충돌 박스

## N5 — 런 완결 (보스 → 승패 → 결과)

- [x] N5-1a 보스 웨이브 — stages.json `boss_at_sec` 시점 단일 보스 스폰,
      상단 얇은 보스 HP바, 처치 = 승리, 타임아웃 = 승리 (GDD §34 소프트
      인레이지는 클리어 후 잔류로 해석)
- [x] **N5-2 자동 저장·자동 재개 + 설정 팝업** — SaveProfile(순수)/
      SaveService(autoload) user://profile.save JSON, temp-rename 안전 쓰기 +
      크래시 복구, schema v1 마이그레이션 훅, 손상 파일 → 경고 + 새 프로필,
      런 종료 시 엽전 뱅킹(결과 화면 "보유 엽전") + 생애 통계, 설정 팝업
      (음량 3슬라이더 + 한국어/English 토글, 즉시 적용·자동 저장),
      Master/Music/Effects 오디오 버스, 타이틀 단일 프라이머리 버튼
      (이어하기/종료 버튼 없음 — 오너 지시)
- [x] N5-1b 결과 화면 — 종이 패널 (승리/패배, 생존 시간, 처치, 엽전
      표시 전용) + 타이틀 복귀 CTA. 메타/뱅킹은 후속 페이즈

## N4 — 전리품 & 무기 개조

- [x] **N4-1 전리품 드랍 + 자석 픽업 + 특수 재료 3택 팝업** — drop_tables
      기반 시드 롤, 티어 tint 다이아몬드 풀링 드랍, XP 오브 자석 경로 재사용,
      RunState 런 인벤토리, 특수 재료 사용(무기 변신·레벨 유지)/보관/분해
      팝업 (행 카드 컴포넌트 공유, 큐잉으로 패널 중첩 금지)

- [x] **N4-2 무기 등급 + 15분 페이싱** — weapons.json `_grades` 사다리
      (일반→고급→희귀→영웅→신화, 단계별 데이터 배수 + mythic tinted 플래그),
      레벨업 `등급↑` 카드 / 개조 등급 승계(max(승계, 결과 기본)), 최상위
      도달 골드 콜아웃(DamageNumber 재사용), bamboo_forest 15분 커브
      (900s 보스, 840s 대량 공세 피크, 5분부터 정예), `bamboo_brute_elite`
      (elite_of 배수 파생 + 전용 희귀 드랍), soft_enrage(920s+, 스폰 시
      스탯 스케일), RunFlow 스케줄 불변식 + validate_data 등급/정예/페이싱
      크로스체크, 보스 hp 7000 재조정
- [x] **N4-6 전리품 자동 획득 + 개조 레벨업 카드 흡수** — 특수 재료 3택
      팝업과 큐잉 삭제 (DESIGN.md §5.2 인터랙션 예산), 모든 재료는 XP·엽전
      처럼 무음 자동 획득 (특수 재료만 데미지 숫자 스타일 플로팅 라벨),
      쓸 수 없는 재료(레시피 없음/결과 무기 보유/개조로 대체됨)는 픽업
      즉시 자동 분해 → 엽전, 재료+기반 무기 보유 시 "개조" 카드가 레벨업
      3택에 등장(화면당 최대 1장, 결과 무기 등급 필 + 실수치), 선택 시
      재료 소모 + 레벨·등급 승계 스왑, **개조로 대체된 무기는 신규·강화
      풀에서 런 내내 영구 제외 (오너 리포트 버그 픽스, 회귀 테스트 포함)**,
      개조 후 죽은 인벤토리 자동 정산
- [x] **N4-2b 5분 런 리스케일** — 오너 결정(모바일 1런 = 5분) 반영:
      bamboo_forest 300s 커브 (2:00 정예, 3:30 대량 공세 피크 57, 4:00 보스,
      4:40 소프트 인레이지, 5:00 타임아웃 승리), XP 곡선 6×1.5^(L-1)로
      런당 레벨업 8~10회, 초반 드랍 테이블 특수 재료 바이어스(첫 런 내
      개조 팝업 보장), 보스 hp 2400, schedule_issues에 duration_sec 초과
      불변식 추가, tools/playtest.tscn 자동 플레이 검증 하네스

---

## 작업 큐 (오너 지시 2026-08-14: 한 번에 하나씩)

병렬 작업은 여기서 끝난다. 아래 순서대로 **하나 개발 → 검증 → 커밋/푸시
→ 오너 확인 → 다음**. 진행 중인 3건(N1-2 타이틀 아트 연결, N2-1 수행자
선택, N4-2 등급·페이싱)이 착지하면 이 큐로 전환한다.

**게이트 (오너 지시 2026-08-14)**: **도사가 완성되기 전에는 두 번째
캐릭터도 두 번째 지역도 착수하지 않는다.** 도사의 스킬 4~5종과 빌드
분기가 실제로 플레이 가능해야 다음 콘텐츠로 넘어간다.

우선순위는 (1) 지금 눈에 보이는 전투 결함 → (2) 도사 완성 → (3) 다듬기
→ (4) 신규 콘텐츠 순이다.

| # | 작업 | 왜 지금 |
|---|---|---|
| 1 | ~~**N3-14 몬스터 겹침 해소**~~ ✅ | 완료 — 그리드 기반 분리 조향, 평균 겹침 8.46 → 0.00 (플레이테스트 261/240 샘플) |
| 2 | ~~**N3-15 조준 범위를 화면 안으로**~~ ✅ | 완료 — 뷰 렉트+마진 내 & 무기 range_px 내 적만 조준, 대상 없으면 발사 보류, 투사체 화면 밖 소멸 |
| 3 | ~~**N4-4a 도사 핵심 무기 6종**~~ ✅ | 완료 — 6종 메커니즘(단일/폭발/연쇄/근접원호/선회도트/관통) + 개조 분기 5종(화령부·뇌정부·봉마검·귀철석장·화령혼불) 데이터 구동. 봉인은 법검 분기로 이동, lightning_talisman 삭제. 플레이테스트: 승리 298.5s, 서지 fps min 59, peak live 51 |
| 4 | ~~**N4-4b 도사 확장 4종 + 액티브 2종**~~ ✅ | 완료 — 결계(장판 도트+감속)·신장(자율 소환, 프롭 충돌)·진언(주기 파동 넉백+기절)·살(사망 전염 저주) + 개조 분기 4종(화염 결계·뇌정 신장·봉인 진언·귀살), 축지(순보+무적)·벽사진(비상 일소) HUD 우하단 클러스터. 그랜트 플레이테스트: 승리 300s, 서지 fps min 59, peak live 58 |
| 4.5 | ~~**N3-16 바닥 렌더 범위 버그**~~ ✅ | 완료 — GroundLayer가 카메라 시야 타일 윈도우를 따라간다 (좌표+시드 순수함수 배리언트/회전, 윈도우 변경 시에만 redraw). 필드 코너 스크린샷으로 회색 밴드 소멸 확인 (tools/ground_check.tscn) |
| A | ~~**N7-2 메타 경제 재설계 + 캐릭터별 특화**~~ ✅ | 완료 (2026-08-16) — 22노드(공용 트렁크 13 + 도사 5 + 무사 2 + 궁수 2), 도사 총액 1,330→14,990냥(~35-40런), 랭크별 증가 비용 곡선을 validate_data가 강제, 신규 노드 종류(엽전/경험치 획득, 2레벨 출정, 4장 선택지, 재료 보장, 피해 감소, 무적 연장, 회생부) 전부 배선, 도사 술법 분기(화상 지속/연쇄 도약/결계 반경/혼불 구슬/봉인 중첩)는 무기 데이터 스탯 재사용, 캐릭터 간 누출 금지 테스트, 잠긴 캐릭터 분기 = 해금 조건 명시 잠금 콘텐츠, 탭 UI. 측정: 뇌부 연쇄 3→5, dps 22.2→32.4 |
| B | ~~**N3-18 기술 이펙트 재작업**~~ ✅ | 완료 (2026-08-16) — 12종 전수 before/after 캡처 진단 후 재작업. 화부: 팩 스프라이트 폐기(마젠타 광선 + 실반경 절반 거짓) → 코드 폭발(골드 코어 플래시→데이터 반경 착지 링+엠버 스파크). 신장 타격: 스프라이트 폐기 → X-슬래시 확대. 뇌부: 첫 타 크랙클(chain_first_leg_px) + 볼트 두께/지속 데이터화 — 이전엔 필드에서 사실상 안 보임. 석장: 이중 실선 → 채운 부채꼴+밝은 리딩 엣지. 혼불: 2.1x 글로우 안개 덩어리 → 히트 반경 캡 글로우+3로브 불꽃+짧은 트레일. 결계: 디버그 원 → 회전 대시 링+팔각 성형 진. 진언/벽사진: 불투명 팬케이크 → WAVE 스타일 이중 링(데이터 반경 정착), 스크린 플래시 0.28→0.14 알파·0.22s. 축지: ACCENT_TAOIST 모듈레이트(야간에 검정) → WEAPON_SOUL. 부적/법검: 투사체 크기 데이터화+비행 트레일. 살: 점프 0.45s. explosion/strike_flash 시트 삭제, blink_puff만 유지. 서지 fps min 59 avg 60(1775 샘플, 기준선 동일), 승리 300s, 테스트 285/285 |
| S | **N9-2 스토리·네이밍 패스** | 오너 지적: 지금 스토리가 전혀 반영 안 된 느낌. GDD 조선 설화 세계관을 실제 텍스트로 내린다 — 도사의 동기와 여정, 지역 배경, 몬스터·보스 유래, 무기·전리품 이름과 한 줄 설명, 화면 문구를 한 목소리로 통일. 화폐 표기(엽전/냥) 정합성도 여기서 정리 |
| 5 | ~~**N4-3 5분 런 밸런스 패스 (측정 기반)**~~ ✅ | 완료 — playtest 강제 빌드(`--weapon`)/배치(`--batch`)/시드/배속 하네스로 10무기 전수 측정. BEFORE 스프레드 35x(혼불 트랩 136dmg) → AFTER 3시드 평균 2.3x. 혼불 궤도 링·진언·신장·결계 구조 수정, edge_falloff/pierce_retention/orb_radius_px 데이터 이관. 서지 ranged 14→18 + enrage 속도 1.45로 순수 회피 봉쇄(노픽 빌드 seed99 사망). 헤비 빌드(6무기 동시) 서지 fps min 59 |
| 6 | ~~**N6-1 첫 실행 흐름(FTUE)**~~ ✅ | 완료 — 첫 부팅 도사 고정 즉시 출정(Ftue.route_character), 조이스틱 이동 힌트 1회(입력 시 해제, 프로필 영속), 첫 개조 카드 1줄 설명("재료로 무기를 바꾼다") 1회, stages.json first_run_drops로 부적지 ≤30s·도깨비불 60s 보장 드랍. 플레이테스트: 힌트 표시→해제, 보장 10.95s/63.9s 발화, 개조 70.3s, 재실행 시 힌트·설명·보장 모두 미발화 |
| 7.5 | ~~**N3-17 카드 오버플로 + 무기 이펙트 품질**~~ ✅ | 완료 — 레벨업 카드가 설명 랩핑 높이에 맞춰 성장(고정 136px 폐지, 최악 데이터 검증 테스트 + tools/popup_check.tscn), 10무기 코드사이드 이펙트 패스(폭발 링/연쇄 번개 볼트/석장 스윕/혼불 글로우+트레일/법검 잔상/결계 틱 펄스/신장 타격 플래시/진언 링+카메라 넛지/살 저주 마크+전파 점프/축지 2점 퍼프/벽사진 전면 플래시), 타이밍 data/effects.json weapon_effects. 후속 아트 통합: Retro Impact Pack 5 → asset/effect/ 3종(폭발/타격 플래시/축지 퍼프, EffectSprite 풀, sprite_effects 데이터, 라이선스 미확인 — 오너 확인 필요), 파라메트릭 이펙트(볼트/링/트레일/마크)는 코드 드로잉 유지 |
| 7 | ~~**N3-13 무기·전리품 아이콘 아트 연결**~~ ✅ | 완료 — 레벨업/개조 카드 웰·보유 무기 스트립이 weapons.json id로 asset/ui 아이콘 바인딩(아이콘 없으면 글자 폴백, 패시브만 해당), 개조 카드에 재료 아이콘 배지, HUD 해골/엽전/일시정지/정보 실아이콘(16px 1x·32px 2x NEAREST), 나무 버튼·종이 패널 9-slice 전환(창살은 패널 텍스처에 내장, GOLD 포커스 링 유지), validate_data가 아이콘 없는 무기/전리품 id 보고. 테스트 205/205, 플레이테스트 승리 300s·보스 처치·서지 fps 60 |
| 7.7 | ~~**N4-7 개조 결과 무기 게이팅 + 레벨업 카드 중복 방지**~~ ✅ | 완료 — 개조 결과 무기는 레시피로만 획득(`evolution_only` 데이터 게이트, validate_data가 weapon_mods 결과 무기의 플래그 누락을 FAIL로 강제), 한 레벨업 화면에 같은 무기 id 카드 2장 금지(LevelUp.pick/assemble subject-id 중복 배제, 200시드 테스트), 보유 무기 스트립 HFlowContainer 랩 + Lv 배지 오버레이로 최대 보유 수에서도 540px 내(레이아웃 테스트). 플레이테스트: 개조 카드 1화면 제시/1회 선택, 신규 카드로 나온 개조 결과 0건, 승리 296.3s |
| 8 | ~~**N5-3 본거지(캠프) 최소판**~~ ✅ | 완료 — 타이틀→캠프→출정 루프 (첫 부팅은 FTUE대로 캠프 생략, 첫 결과 화면부터 캠프 착지), 보유 엽전+생애 통계 표시, GDD §24 건물 4곳 준비 중 플레이스홀더, 수행자 선택 캠프 동선, 순수 Camp 헬퍼 + 테스트 6종, tools/camp_check.tscn 스크린샷 |
| 9 | ~~**N7-1 명부수형 메타 트리**~~ ✅ | 완료 — data/meta_tree.json 8노드 DAG(단계별 비용, 선행 조건, 스탯 캡), 순수 MetaTree 헬퍼(구매=단일 프로필 fold, 손상 상태 새니타이즈, 캡 적용 집계), 프로필 schema v2(meta_tree, 신형 스키마 읽기 전용 페일세이프, 엽전 오버플로 클램프), 런 시작 1회 적용(무기 배율/이속/자석/HP), 명부수 화면(설화 `_02` 문법: 신목 트렁크+노드 그래프 스크롤, 상세 카드=다음 단계 표시, 나무 CTA, 최대 시 CTA 숨김), 캠프 명부수 스팟 라우팅, validate_data 사이클/미지 스탯/중복 id/도달 불가 거부, 테스트 245/245, 맥스 트리 플레이테스트: seed7 일반 봇 224s 패배·seed123 nopick 200s 패배(캡 유효) |
| 10 | ~~**N5-4 괴이록(도감)**~~ ✅ | 완료 — 발견 즉시 영구 기록(몬스터 처치/전리품 획득/무기 보유·개조 완성, 런 중 즉시 저장이라 패배·크래시에도 생존), 프로필 bestiary 블록(v2 유지, 무기록 프로필 빈 기록 마이그레이션), 캠프 괴이록 스팟 → 3탭 화면(몬스터/전리품/무기, 행 카드, 미발견 = ??? + 실루엣, 섹션·전체 카운트는 데이터 파생), 발견 몬스터 = 출몰 지역, 재료 = 티어 pill + 변환 레시피(미발견 쪽 ??? 마스킹), 무기 = 메커니즘 1줄 + 개조 분기, 캠프 새 기록 1줄 힌트(열람 시 해제), 미지 id 로드 시 경고 드랍, 테스트 259/259 |
| 10.5 | ~~**QA-1 전체 기능 감사**~~ ✅ | 완료 (2026-08-16) — 전 기능 pass, 수정 3건(물웅덩이 통행 가능화, 결과 화면 보스 HP바 잔류 제거, playtest 보스 리포트 오표기), 큐 후보 리포트 4건: 부분 로컬라이제이션(캠프/결과/HUD/카드 한국어 고정), 캠프에서 설정 접근 불가, .import 사이드카 정책 불일치(24 추적/85 미추적, CI.md와 모순), _end_run null 가드 비일관 |
| 10.7 | ~~**QA-2 신규 유저 경험 감사 + .import 정책**~~ ✅ | 완료 (2026-08-16) — .import 사이드카 untrack(폰트 1건만 화이트리스트, fresh-clone 검증), 소수정 3건(HUD 죽은 ⓘ 버튼 숨김, 카드 문구 px 단위 제거, 명부수 CTA 잔액 부족 시 비활성 스타일), 큐 후보: 첫 90초 무위협 구간, 사망 원인 미표시, HUD HP 부재, 승리 시 보스 생존 앤티클라이맥스, 캠프 아트 부재, 패시브 글자 아이콘, 레어리티 필 단색, 액티브 스킬 무설명, 괴이록 NEW 뱃지 부재 |
| 10.8 | ~~**N6-2 오프닝 위협 + 사망 가독성 + HUD HP**~~ ✅ | 완료 (2026-08-16) — QA-2 발견 1·2·3 해소. (1) 오프닝 프론트로드: 0s 고블린 14@0.7s + 12s 10@0.8s (기존 5@2.5s), stages.json `opening` 블록(20s 내 최소 18스폰·40s 내 첫 레벨업 XP 계획)을 RunFlow.opening_issues로 validate_data가 강제, 보장 드랍은 이동 방향 160px 앞 착지. 측정: 일반 봇 첫 레벨업 33.9s·0-20s 피해 0·승리, idle 봇 0-20s 피해 30·33.2s 사망. (2) 죽음 라인: Enemy name_ko → Player.last_hit_source → 결과 화면 "죽음" 행(정예/보스 이름 통과, 무귀속 중립 폴백), 저HP 경고 = 데이터 임계 25%·0.9s 루프 vignette. (3) HUD HP바: XP바 아래 5px 스트립(토큰 색, 저HP 시 주홍), 스프라이트 아래 바 유지(시선 위치 보완). 테스트 275/275 |
| 10.9 | ~~**N4-8 무기 성장 곡선 재조형**~~ ✅ | 완료 (2026-08-17) — 도사 무기 21종(기본 10 + 개조 결과 11) 성장 곡선 재설계: 1레벨 겸손(기본 피해 ~70-77%로 컷), 레벨당 체감(+25-40%/레벨), `milestones` 스키마(레벨별 메커니즘 추가 성장 — 투사체/연쇄/원호/구슬/장판/전염, LevelUp.stats_at_level 병합, 카드에 ★표기, validate_data가 경로 화이트리스트·mid+max 필수·max 병합 스탯 계약 강제). 멀티샷 훅(WeaponMath.fan_directions), AutoWeapon 레벨별 메커니즘 재계산, 스포너 live_cap 화면 밖 최원거리 재활용(제로 dps 회피 런이 wave 일정을 얼리던 구멍 폐쇄), enrage 250s/1.6배속+escort 연장. 헤드룸 x2.1-3.9 → x3.9-15.6, 무강화 런 3/3 사망(216-256s), 일반 런 승리(38-41 dps), L8 앵커 스프레드 2.1x(노이즈 플로어 내), maxed meta+nopick 8시드 1사망(가드 유지). 테스트 294/294, 스크린샷 captures/n4-8/ |
| 10.95 | ~~**N4-9 진화 획득화(희소성+지식)**~~ ✅ | 완료 (2026-08-17) — 특수 재료 잡몹 드랍 사실상 제거(도깨비불 0.005만), 정예가 공급원(합 0.12/마리, 화령석 0.08 중심), drop_tables `_config.special_chance_max` 캡을 validate_data가 강제(잡몹 0.005/정예 0.12), 전 레시피 `level_required: 3`(N4-8 첫 마일스톤 재사용, max_level 초과·마일스톤 이탈 FAIL, FTUE 첫 런만 면제), 명부수 천운 노드(특수 확률 +25%×2, 캡 0.5, 특수만 배율), 미수행 진화는 카드에서 ??? 마스킹 → 수행 시 괴이록 레시피 공개(기존 기록 재사용, 지식만·할인 없음). 측정(20런 시드): luck0 특수 0.55/런·8/20런, 천운max 0.90/런·13/20런(정확히 ×1.5), 메타max 진화 2/10런, FTUE 보장 1/1 유지. 스크린샷 captures/n4-9/ |
| 10.97 | ~~**N5-5 파괴 오브젝트 + 정예 보상 상자**~~ ✅ | 완료 (2026-08-17) — 파괴 가능 프롭 3종(소죽림 25/바위 45/쓰러진 나무 60 HP, 데이터 구동, 장식은 불가·validator 강제), 플레이어 무기만 피해(투사체/석장 원호/혼불 구슬/진언 파동 — 적은 불가, 의도), 파괴 시 풀링 퍼프 + 픽업 테이블 롤(꽝 50/엽전 38/회복 6/전멸부 3/자석 3, 꽝+엽전 ≥80% validator 강제), 픽업은 자석 경로 월드 엔티티. 회복 만피 시 10냥 전환(획득 시점 판정), 전멸부는 화면 내 잡몹 999·정예/보스 150 캡(데이터, 원샷 방지 validator·보스 2400→2250 생존 증명), 자석은 전 필드 흡인(빈 필드 무해). 정예 처치 = 풀링 상자 드랍, 밟아서 개봉 = 시간 정지 + 보상 1/3/5(기본 70/22/8, 천운 luck×shift 0/4/10 벤드 — 10k 측정: luck0 69.5/22.8/7.8%, luck0.5 38.5/35.4/26.1%), 보상은 레벨업 풀 기계 재사용(카드당 1탭, 5보상=5탭, 고갈 시 40냥), 런 종료 후 개봉은 폐기(일시정지 잔류 금지). 측정: 5런 파괴 평균 6.4/런, 상자 평균 3.0/런, 렌더 서지 fps min 59 avg 60(기준선 동일). 스크린샷 captures/n5-5/ |
| 10.98 | ~~**QA-3 신규 유저 경험 감사 2차**~~ ✅ | 완료 (2026-08-18) — 밸런스·콘텐츠 웨이브 이후 재감사 (프레시 세이브 실플레이 + 렌더 봇 런, 스크린샷 captures/qa-3/). 소수정 2건: 등급 필 단계별 색상(팔레트 토큰, 문자 우선 유지), 비활성 나무 버튼 텍스트 가독성(엽전 부족 CTA). **미검증 1건**: 오프닝 러시의 인간 반응속도 체감 난이도 — idle 봇 ~30s 사망(의도), 퍼펙트 봇 무피해 승리 사이 실제 신규 유저 체감을 데스크톱 사용 충돌로 실측 못 함, 오너 30초 자가 확인 요청. 구조적 리스크: 첫 레벨업 팝업(~0:23)이 러시 수렴 시점과 정확히 겹침 — 팝업 닫는 순간 포위 상태. 큐 후보: (1) 오프닝 팝업 직후 1.5-2s 접촉 유예 검토, (2) 회복 루프 부재(파괴물 회복 5.8%뿐, 봇 승리 런에 1회), (3) 파괴 오브젝트 시스템 비가시(부술 수 있다는 신호 전무, 꽝 50%), (4) 신장 소환수 = 파란 사각형(스프라이트 부재, 유일하게 미완성으로 보임), (5) 진화 인런 신호 부재(재료 0.55/런 + ??? 카드 무수치 블라인드 픽), (6) HP/XP 얇은 이중 스트립 혼동, (7) 보스 HP바 무명·최상단 얇음, (8) 상자 5보상 = 레벨업 화면 5연속(잭팟감 없음), (9) 카드 소수점 수치(12.65), (10) 첫 카드 골드 포커스 링이 추천으로 읽힘, (11) 캠프 준비 중 스팟 무반응 의심 + 무기 도감/괴이록 무기 탭 중복. QA-2 잔존 확인: 보스 생존 타임아웃 앤티클라이맥스(완화됨, N4-8 후 정상 빌드는 보스 처치), 캠프 텍스트 메뉴(유지), 패시브 글자 아이콘(유지), 액티브 무소개(유지, 오프닝 난이도로 더 아픔), ~~레어리티 필 단색~~(이번 수정), NEW 뱃지 부재(유지), 명부수 지출 넛지 부재(유지), 엽전/냥 표기 혼재(유지), 일시정지 설정 부재(유지), 부분 영문화(유지) |
| 10.99 | ~~**N6-3 팝업 유예 + 회복 루프**~~ ✅ | 완료 (2026-08-18) — QA-3 발견 1·2 해소. (1) 팝업 접촉 유예: 모든 선택 화면(레벨업/상자, 큐 경유 전부)이 닫혀 일시정지가 풀리는 순간 1.5s 접촉 무적(effects.json `popup_grace_sec`, 기존 무적 블링크로 텔레그래프). 큐 드레인당 1회, CombatMath.grace_extend가 maxf 갱신이라 중첩·이월 불가(순수 헬퍼 + 테스트 4종). 렌더 증거: 첫 팝업 ~25s 닫힘 직후 유예 활성 샷. 오프닝 2펄스는 유지 — 유예가 결정 순간과 위험 피크를 분리하고, 웨이브 재배치는 QA-3 확인된 0:10 긴장(validator 오프닝 계약)을 깨는 리스크라 기각, idle 프로브 0-30s 피해 102.0·33.2s 사망 전후 동일. (2) 회복 루프: 파괴물 회복 6→10%(꽝 50→46, plain 84%≥0.8 validator 유지) + 정예 처치 시 max HP 15% 회복(pickups.json `elite_heal`, (0,0.5] validator, 공유 힐 경로 — 2:00 정예가 바로 누적 피해가 치명이 되는 지점이라 정예 격파 = 회복 밸브, UI 0, 예산 상한 6마리, 보상이지 드립 아님). Pickups.heal_budget 순수 헬퍼로 예산 산정(테스트 3종). 측정: 일반 봇 힐 0→6회 113.4hp(예산 161.3), hp@2:00 96→96/150, hp@보스 96(회복 불가)→122.4/150(회복), 서지 fps min 60 avg 60(기준선 유지). 가드: 유예가 나쁜 빌드도 버프해 metamax nopick이 enrage 2.5에서 무패(12/12) → `soft_enrage.damage_mult_max` 2.5→2.8 보상, 최종 데이터에서 nopick 293.3s 패배·metamax nopick 260.5s 패배 관측(시드 고정에도 결과는 타이밍 노이즈, N4-8의 1/8 기준과 동일)·일반 봇 270.8s 보스킬 승리. 정예 힐 런타임 증명 50.4→69.3(+15%). 테스트 317/317, 스크린샷 captures/n6-3/ |
| 10.995 | ~~**N6-4 유예 철회 + 플로팅 조이스틱 + 무발사 금지**~~ ✅ | 완료 (2026-08-18) — 오너 지시로 N6-3 팝업 접촉 유예 철회 + 실제 요구 3건 구현. (1) 유예 삭제: `popup_grace_sec` 데이터·Stage 팝업 닫힘 그랜트·validator 필드 제거 — 선택 화면은 트리 전체 정지가 이미 올바른 모델, 정지 위에 무적을 얹는 건 잘못된 처방(오너). 축지·회생부가 쓰는 타임드 실드 배관(grace_extend/grace_tick, _bonus_invuln_left)은 유지, 테스트는 타임드 실드 계약으로 재라벨. 유예 보상으로 올렸던 `soft_enrage.damage_mult_max` 2.8→2.5 원복 — 재측정으로 확인(아래). 유예가 덮던 진짜 문제(러시 수렴 ≈ 첫 레벨업 겹침)는 웨이브 재배치로 해소: 2펄스 12s 10@0.8 → 17s 10@0.7 (validator 오프닝 계약 19스폰≥18 유지, 0s 14@0.7 첫 펄스 = 0:10 긴장 불변). 렌더 1x 측정: 팝업 닫힘 시 근접(250px) 적 11 → 5마리, 수렴-팝업 마진 확보, idle 0-30s 피해 102→108(오프닝 여전히 치명), 가드 plain nopick 219.1s 패배·metamax nopick 시드 스윕 패배 관측·일반 봇 승리. (2) 플로팅 조이스틱: 화면 아무 곳 터치 = 그 지점이 스틱 원점, 릴리즈 = 숨김, 휴면 패드는 좌하단 힌트로 유지(FTUE 이동 힌트가 가리키는 앵커), HUD 버튼 탭 우선권(CombatHud.blocks_point 재귀 히트테스트), 두 번째 손가락이 이동을 못 끊음(캡처 인덱스 고정), WASD 불변, 순수 스틱 수학 statics + 테스트 5종. (3) 무기 무발사 금지: N3-15 "대상 없으면 발사 보류" 폐지 — CombatMath.fallback_aim 단일 체인(가시 적 in range → 가시 파괴물 → 바라보는 방향으로 발사)을 투사체·석장 원호·결계 전부에 일괄 적용(무기별 특례 0), 투사체 화면 밖 소멸 불변, 테스트 5종 + tools/n64_check 런타임 증명(적 0마리에서 프롭 사격 60→49, 전무 시 전방 발사 heading(0,-1)). 테스트 327/327, 스크린샷 captures/n6-4/ |
| 10.996 | ~~**N6-5 골드 원천 정리 + 조이스틱 옵션 + 필드 정돈 + 에셋 와이어링**~~ ✅ | 완료 (2026-08-18) — 오너 요청 4건. (1) 몬스터 골드 폐지: monsters.json gold_drop 23종 전부 삭제(처치 = XP+전리품만), 보스 150은 stages.json `boss_gold` 보스킬 직불로 이전(킬 즉시 런 종료라 상자는 열 수 없음 — 직불 선택), validator가 gold_drop 부활을 FAIL. 잔여 원천 상향(파괴물 골드 12→40, 만피 회복 전환 10→25, 상자 폴백 40→60, salvage 5/10/18/32) + 명부수 비용 ~47% 스케일(도사 관련 14,990→7,945냥, 루트 30/35냥). 측정(7런): 보스킬 승리 380-440·타임아웃 승리 25-155·패배 15-50, 혼합 ~163냥/런 → 봇 기준 ~49런, 인간 추정 35-49런(30-50 목표 내). (2) 조이스틱 불투명도 설정: settings 팝업 4번째 슬라이더, 프로필 영속(`joystick_opacity`), 드로우 타임 적용(라이브), 클램프 [0.2, 1.0] 기본 1.0 — 실수로 완전 투명 불가. (3) 조이스틱 중립톤: JOYSTICK_BASE/RING/KNOB 잉크·탈채도 라이트 토큰 신설, wood 계열 탈피(버튼과 경쟁 안 함). (4) 필드 정돈: 솔리드 60→40·데코 110→48을 9개 시드 클러스터 디스크(r170, 중심 간 420px, 스폰링 밖)로 군집화 — 열린 차선 확보, 시드 재현·스폰클리어·60px 간격 유지(신규 테스트), 지면 변형 타일 10-20%→4-5% 측정(임계 0.27→0.42) + 베이스 위 0.55 알파 블렌드, 브레이크/런 16→평균 2.7(0-9). (5) 에셋: 엽전 픽업 = HUD coin.png 재사용, 회복 = Pixel UI pack 3 하트, 폭탄 = Trap and Weapon Bomb(asset/pickups/build_assets.py 파이프라인, 야간 틴트, 1:1 픽셀), 자석·정예 상자·신장 몸체는 팩에 없음 — ASSET_REQUIREMENTS 등록(상자 = 차기 생성 1순위), 라이선스 UNCONFIRMED 행 2건. 테스트 329/329, validate PASS, 스크린샷 captures/n6-5/ |
| 10.999 | ~~**N9-22 난이도 사다리 + 밤 길이 설정**~~ ✅ | 완료 (2026-08-19) — 전략 감사 결론(단일 스탯 메타 곡선은 최대치 플레이어를 긴장시킬 수 없다)의 직접 대응. data/difficulties.json 5단계 사다리(순행/여행/수난/악연/역병, rank 오름차순 고유값, 클리어해야 다음 해금)가 적 체력·공격력·이속, 스폰 수, 정예 웨이브 수, 보스 체력을 곱해 올리고 엽전·경험치를 그만큼 더 준다. 밤 길이 3종(짧은 밤 0.6 / 보통 밤 1.0 / 긴 밤 1.5)은 지속시간만 자르는 게 아니라 웨이브 시각·서지·보스·격노 시작을 함께 늘려 밤의 형태를 보존한다. Difficulty = 순수 static 헬퍼(사다리·해금 체인·스케일 fold 전부 헤드리스 테스트), validate_data가 데이터 계약 강제(고유 rank, 해결 가능한 unlock_after, 루트 정확히 1개, 양수 배수, default가 스스로 열려 있을 것). spawner와 stage가 같은 배율을 읽어 HUD 시계와 웨이브 일정이 어긋나지 않는다. 승리 시 SaveService.mark_difficulty_cleared로 티어 기록(중복 시 디스크 쓰기 없음). 캠프 출정 위 순환 버튼 2개(잠긴 티어는 순환에 없음, 안 건드리면 출정은 여전히 1탭) — 하단 여백 48→28로 조정해 540x960에서 출정 버튼 잘림 해소. 테스트 340/340, validate 16파일 PASS, 헤드리스 플레이테스트 승리 379.9s·스크립트 에러 0 |
| 10.9995 | ~~**N9-23 빌드 슬롯 + 개조 5레벨 게이트**~~ ✅ | 완료 (2026-08-19, 오너 지시) — 오너 지적 "무기 5렙에 개조 열어달라했는데"가 맞다. 앞서 5로 넣었다가 도달 불가로 측정돼 3으로 되돌렸고, **그 되돌림을 오너에게 확인받지 않은 것이 잘못**이다. 이번에 원인까지 같이 고쳤다. 원인: 레벨업 후보 풀에 보유 무기 강화 + 미보유 무기 전부 + 패시브 12종이 균등하게 들어가, 안 고른 것들 때문에 풀이 계속 커져서 특정 무기 강화 카드가 한 판에 평균 3회만 떴다 — 잘 해서 5를 찍는 게 아니라 확률로 막혔다(측정 천장 4). 이차 희석: `pick`이 subject id 기준 중복을 막으므로 같은 무기의 등급업 카드가 강화 카드와 같은 자리를 두고 경쟁한다. 조치: **무기 4칸 / 패시브 4칸 슬롯**(가득 차면 신규 카드만 중단, 보유분 성장은 계속. 0스택 패시브는 슬롯 미소모. 전부 만렙이면 슬롯 재개방해 빈 화면 방지) + **19개 레시피 전부 `level_required` 5**. 측정: 랜덤 봇 최고 무기 레벨 4→8, 보유 무기 5+→최대 4; 강제 빌드(`--weapon=old_talisman`) 3시드 중 2시드가 낡은 부적→화염 부적→봉황 부적 2연속 개조 완주(Lv.8/Lv.5), 나머지 1시드는 특수 재료 0개라 게이트와 무관하게 불가. 승패 대역 유지(승리 366-420s, 패배 209-312s). 가독성: 일시정지에 `무기 n/4`·`패시브 n/4` + (가득 참) 표기(안 그러면 신규 카드 중단이 버그로 읽힘), 개조 경로에 `Lv.N 필요` 접미(재료만 있고 레벨 미달일 때 빈 ✓가 뜨던 문제). 테스트 354/354, validate 16파일 PASS, data/BALANCE.md 기록 |
| 10.9996 | ~~**N9-24 개조 재료 공급 상향 + 바닥**~~ ✅ | 완료 (2026-08-19, 오너 지시 "재료 드랍률도 좀 올려줘") — N9-23으로 게이트가 5가 되면서 병목이 투자에서 공급으로 옮겨갔다. 엘리트 특수 합계 0.40→**0.70**(검증기 캡 상한에 이미 붙어 있어 캡도 같이 이동), 잡몹(숲 정령 도깨비불) 0.005→**0.015**(캡 동반). **보스는 의도적으로 안 올림** — 보스 처치가 같은 호출에서 런을 끝내므로(N6-5) 드랍을 주울 수 없다, 올려도 플레이어가 볼 수 없는 숫자다. 평균만 올려선 재료 0개로 끝나는 판이 안 없어져서(그런 판은 게이트가 몇이든 개조 불가) **런의 첫 정예 처치에 특수 재료 1개 보장** 추가 — 자체 굴림이 이미 특수를 냈으면 미발화, 보장 재료는 그 몬스터 드랍표의 확률 비중대로 뽑아 표가 줄 생각 없던 재료가 나오지 않는다(`Loot.weighted_special`, 유닛 5종). 측정(각 10판): 랜덤 봇 특수 27개(8/10판)→**48개(10/10판)**, 런당 2.7→**4.8**; 강제 빌드 개조 5→**9(4/10판)**. **남은 마른 판은 드랍률 문제가 아니다** — seed 2001 프로브 결과 372.7초까지 생존하고도 정예 처치 0, 단일 무기 빌드가 6배 HP 정예를 못 뚫어 보장이 발화하지 않는다(설계대로). 화력 부족→재료 없음→개조 불가→계속 약함의 루프이며, 깰 거면 레버는 추가 확률 상향이 아니라 시간 기반 백스톱(3:00까지 무조건 1개) 또는 파괴 오브젝트 소량 드랍 — 오너 판단으로 남겨둠. 부수 기록: 랜덤 봇 개조가 4/10판→1/10판으로 감소, 이는 Lv.5 게이트가 의도대로 작동한 결과(분산 투자로는 5레벨에 못 닿는다). 테스트 359/359, validate 16파일 PASS |
| 10.9997 | ~~**N9-25 일시정지 능력치 패널**~~ ✅ | 완료 (2026-08-19, 오너 요청) — 일시정지에 캐릭터 상세 12줄(체력·이동 속도·공격력·공격 속도·치명타 확률·치명타 피해·투사체·투사체 속도·피해 감소·자석 범위·경험치 획득·행운) 2열 표시. **표시값은 전투가 쓰는 식을 그대로 재사용**(`_refresh_run_scalars`/`_refresh_weapon_scales`와 동일 표현식) — 별도 계산 사본을 두면 어느 한쪽만 튜닝될 때 표시와 실제가 갈라진다. 피해 감소는 실제 적용되는 50% 바닥을 반영해 표시(원시 합계를 보여주면 받지 못할 감소를 약속하게 됨). 기본값에서 움직인 항목만 잉크색, 나머지는 muted — 이 런이 무엇을 만들었는지가 숫자를 읽지 않아도 보인다. 체력만 예외로 항상 잉크(런 중 실제로 확인하는 유일한 줄). 검증: pause_check 하네스에 12줄 픽스처 + **패널이 960px 화면을 안 넘는지 어서션 추가**(최악 케이스 774px 측정), playtest에 실제 런 상태로 시트를 만들어보는 프로브 추가(일시정지는 사람이 눌러야만 그려지므로 없으면 포맷 오류가 탭 순간의 크래시로만 드러난다 — 실측 12줄/5개 이동). 테스트 359/359, 스크린샷 captures/n9-25/ |
| 10.9998 | ~~**N9-1a BGM 연결**~~ ✅ | 완료 (2026-08-19, 오너가 `new_asset/bgm/`에 3곡 투입) — 타이틀(조선라이크 292s)·본거지(216s)·대나무숲(216s)을 `asset/bgm/{title,camp,bamboo_forest}.mp3`로 이관, `data/audio.json` 구동. `MusicService` autoload(MusicManager): 플레이어 2개로 크로스페이드(1.2s), **호출부는 파일이 아니라 track id를 부르고 같은 id 재요청은 no-op** — 그래서 같은 곡을 쓰는 화면 전환에서 곡이 다시 시작하지 않는다. **스테이지는 stage id를 그대로 track id로 쓰므로** 2번째 지역은 audio.json 한 줄 추가로 자기 음악을 갖는다. 기존 Music 버스에 실어 설정의 음악 볼륨 슬라이더가 곧바로 동작. 오디오 파일이 없어도 경고 1회 후 무음으로 정상 진행(음악이 빌드 실패 사유가 되면 안 된다). **런타임 검증에서 실제 버그 2건 발견·수정**: (1) `load()` 캐시가 stop() 이후에도 스트림을 붙들어 모든 하네스에 `resources still in use at exit` ERROR — CACHE_MODE_IGNORE로 전환(캐시된 리소스에 loop 플래그를 써넣던 전역 부작용도 제거, 현재 곡만 상주하므로 모바일 메모리에도 유리, 3곡 합 ~17MB), (2) `linear_to_db(0.0)`이 -inf라 페이드 보간 첫 스텝에서 NaN → 페이드 프레임마다 `Volume can't be set to NaN` (초기 구현부터 있었고 grep 필터에 가려져 있었음) → 유한 바닥 -60dB. mp3 루프는 .import 사이드카가 아니라 스트림 속성으로 설정(QA-2 정책상 사이드카 미추적이라 fresh clone에서 루프가 꺼진 채 재임포트된다). tools/music_check.tscn 런타임 증명(3곡 재생·Music 버스·루프·동일곡 무재시작·stop 무음), 테스트 365/365, validate 17파일 PASS, 라이선스 UNCONFIRMED 1행 등록 |
| 10.9999 | ~~**N9-26 웹(WASM) 빌드 + Vercel 배포**~~ ✅ | 완료 (2026-08-19, 오너 요청 "친구들이랑 같이 테스트") — `Web` 익스포트 프리셋 + `vercel.json` + `.github/workflows/web-deploy.yml`. **Godot 4 웹 런타임은 SharedArrayBuffer를 쓰므로 COOP/COEP 헤더 없이는 아예 시작하지 않는다** — vercel.json이 전 응답에 두 헤더를 넣고 익스포트 디렉터리로 복사된다. wasm/pck는 immutable 캐시, index.html은 no-cache(재배포 즉시 반영). **빌드는 항상 돌고 배포만 조건부** — VERCEL_TOKEN이 없으면 안내 후 스킵하되 아티팩트는 올라간다(포크·시크릿 없는 클론이 빨간불이 되지 않고, 시크릿 전에도 받아서 서빙 가능). 산출물 31MB(압축), 그중 BGM 3곡이 ~17MB. **CI 3회 왕복하며 실제 결함 2건 발견**: (1) 콜드 `.godot`에서 `asset/ui_theme.tres`가 폰트보다 먼저 파싱돼 `Parse Error: referenced non-existent resource` — 임포트 2회 실행 후 두 번째 패스만 검사하도록 수정(run 32218173780). **`ci.yml`은 여전히 단일 패스이며 `.godot` 캐시가 따뜻해서 통과할 뿐 — 캐시 만료 시 main CI가 같은 이유로 실패한다(미처리)**. (2) 웹 프리셋의 VRAM 텍스처 압축이 ETC2/ASTC 프로젝트 설정을 요구해 익스포트가 "configuration errors:" + 빈 목록으로 실패 — 2D 픽셀아트엔 화질만 깎아서 해제. 익스포트 성공을 exit code로 믿지 않고 index.html/wasm/pck 존재·비어있지 않음을 검사(wasm 없는 익스포트 = 백지 배포). 템플릿 목록 출력 단계 추가(템플릿 누락과 옵션 오류가 둘 다 "configuration errors"인데 전자만 파일명을 찍는다). 검증: run 32218470252 전 단계 성공. **오너 필요 작업**: Vercel 빈 프로젝트 생성(GitHub 연동 금지 — Vercel은 Godot을 빌드 못 한다) → 토큰 발급 → 시크릿 3종(VERCEL_TOKEN/ORG_ID/PROJECT_ID) 등록. 한계: 세이브가 IndexedDB(브라우저별·시크릿창별, 사이트 데이터 삭제 시 소멸), 데스크톱 브라우저에서 540x960 레터박스. docs/CI.md에 전량 기록 |
| 10.99995 | ~~**N9-27 등급 축 차별화 + 등급 가시화**~~ ✅ | 완료 (2026-08-19, 오너 지적 "등급 올리는거랑 레벨 올리는거랑 별개로 되어있는데 이게 맞아?") — 진단 3건: (1) 두 축이 같은 숫자를 밀었다(등급 = 데미지·쿨다운 배수 = 레벨이 하는 것, 등급엔 마일스톤 같은 고유 효과 0), (2) 일시정지가 `Lv.N`만 찍어 **등급이 화면에 안 보였다**(등급↑을 골라도 확인 불가), (3) `pick`이 subject id로 중복을 막으므로 **등급 카드가 그 무기의 레벨 카드를 그 화면에서 밀어낸다** — 측정 런당 등급 1~2회 = 레벨 1~2회 손실, N9-23의 Lv.5 개조 게이트와 정면 충돌. 조치: 등급 = **치명타 특화**(고급 확률 +7%, 희귀 피해 +40%, 영웅 확률 +7%, 신화 피해 +60%), 데미지·쿨다운 배수 전면 제거. **크리를 고른 이유**: `AutoWeapon._roll_damage` 단일 경로가 투사체·원호·결계 틱·궤도까지 전부 덮는 유일한 질적 노브다 — 관통·폭발이었으면 결계·혼불·신장에 죽은 등급이 된다. 배선 중 **신장 소환수가 크리를 아예 안 굴리던 누락 발견·수정**(결계처럼 공격마다 굴림, 소환 시 1회 굴림은 평생 고정이라 부적합). 상한: 확률 44%(패시브 30 + 등급 14), 배수 4.0배(기본 2 + 패시브 1 + 등급 1) → 완전 특화 시 평균 2.3배, 패시브 10픽 소요라 무료 아님. 가시화: 일시정지 무기 칸 테두리 = 등급색 + 등급명 병기(색만으로 정보 전달 금지 규칙), 등급↑ 카드가 실제 부여 효과를 말한다(기존엔 이제 안 변하는 "피해 X→Y"를 출력), 등급색 테이블을 `UiPalette.grade_color`로 통합(두 화면이 읽게 되어 사본 2개는 드리프트 원인). validate_data: 각 단계는 `mult` 또는 `add` 중 하나로 **무언가는 줘야 하고**(아무것도 안 주는 단계 = 이유 없이 레벨업을 쓰는 카드), `add` 필드는 런타임이 읽는 것만 화이트리스트. 측정 4시드 승리 2(376.8s/420.1s)·패배 2(295.9s/172.1s, 통상 대역). 테스트 367/367, validate 17파일 PASS, 스크린샷 captures/n9-27/ |
| 10.99996 | ~~**N9-28 등급 = 만렙 이후**~~ ✅ | 완료 (2026-08-19, 오너 지시) — 등급 카드는 무기가 max_level에 도달한 뒤에만 뜬다. N9-27이 기록한 충돌도 함께 해소 — 한 화면에 subject id당 카드 1장이라 등급 카드가 그 무기의 레벨 카드를 밀어냈고(런당 1~2회 = 레벨 1~2회 손실) Lv.5 개조 게이트와 부딪쳤다. 순서를 플레이어가 중재하게 두지 않고 명시한다: 레벨을 다 올린 뒤 특화. 테스트 2건은 느슨하게 푸는 대신 계약에 맞춰 이동(등급 제공 테스트는 먼저 만렙을 찍어 이름값대로 등급 단계를 재고, "subject당 1장" 테스트는 candidates()가 더는 그 충돌을 못 만들므로 풀을 손으로 구성 — 검사 대상은 pick()의 규칙이다). **측정된 대가**: 랜덤 봇 기준 등급 선택 전 시드 0회. max_level 8, 런당 레벨업 ~19회, 무기 4칸이라 등급은 한 무기에 전념했을 때의 후반 보상이 됐다. 의도적으로 고르는 사람이 닿는지는 **미측정** — 봇은 무작위로 찍고 `--weapon`은 빌드를 부여하는 계측 모드라 집중 픽커를 흉내내는 하네스가 없다. 실플레이에서 너무 멀면 레버는 게이트가 아니라 max_level. 테스트 370/370 |
| 10.99997 | ~~**N9-29 레벨마다 무기 고유 성장**~~ ✅ | 완료 (2026-08-19, 오너 지시) — N4-8 `milestones` 기계는 있었으나 3·5·8 세 지점만 발화해 2·4·6·7은 데미지·쿨다운만 움직였다. 도사 무기 21종 전부 **2~8 모든 레벨**에 자기 메커닉 블록 안의 성장을 부여. 끝값: 살 전이 3→7마리·전파 110→136·저주 5→9, 뇌부 연쇄 3→7·거리 150→185, 뇌정부 감전 지속 1.5→2.3, 화부 폭발 90→132, 화령부 화상 5→9, 결계 45→64·3.5→4.5초, 석장 원호 160→250°, 혼불 구슬 2→4, 신장 지속 11→15초·공격주기 0.65→0.55, 진언 130→168·기절 0.8→1.2, 법검 투사체 1→4, 낡은 부적 투사체 1→3·관통 0→5. **브리프 정정 1건**: 오너가 말한 "화부 화상 데미지"·"뇌부 감전"은 실제로 **개조 결과**의 것이다(화부엔 burn 블록이, 뇌부엔 shock 블록이 없다 — 화령부·뇌정부에 있다). 각 무기는 자기가 가진 킷 안에서만 자라고 상태이상 성장은 개조가 가져간다 — 안 그러면 화부가 화상을 얻는 순간 화령부가 다른 무기일 이유가 사라진다. **함정 2건 발견**: `_merge_delta`가 없는 블록을 만들고 0 기본값에 더하므로, 런타임 기본값이 0이 아닌 필드에 마일스톤을 걸면 조용히 오작동한다 — `projectile_count`(기본 1)는 선언 안 한 무기에서 0+1=1로 **아무 변화 없는 죽은 마일스톤**, `pierce_retention`(기본 1.0)은 0+0.05=0.05로 **업그레이드를 가장한 치명적 너프**. 전수 감사 결과 출고 데이터엔 해당 없음. 작성한 표는 검증기가 이미 지키는 범위 안(원호<360, pierce_retention 정확히 1.0 상한)이고 `slow_scale`(낮을수록 강함)은 건드리지 않았다. 테스트 370/370, 3시드 통상 대역 |
| 10.99998 | ~~**N9-30 살 전파 연출 = 저주답게**~~ ✅ | 완료 (2026-08-19, 오너 지적 "번개처럼 퍼져서 뭔가 더 저주처럼 퍼졌으면") — 원인: 살의 전파가 **뇌부의 `ChainBolt`를 그대로 재사용하고 색만 보라로 바꾼 것**이었다. 번개는 내리치고 저주는 기어간다. 전용 `CurseCreep` 신설 — 사인 곡선으로 늘어진 구슬 실이 죽은 숙주에서 새 숙주로 **순차적으로 기어가고**(구슬이 진행도를 넘어야 나타나는 스태거가 creep과 strike를 가르는 지점), 도착하면 확산 링으로 피어난다. 양 끝 sag가 0이라 두 시체에 정확히 고정되고, 구슬은 끝으로 갈수록 작아져 방향이 읽힌다. 기하는 순수 static이라 헤드리스 테스트 6종(양끝 고정, 중앙 처짐, 처짐 최대점, 좌우 컬, wobble 수직 전용, 길이 0 NaN 방지 — Vector2.orthogonal 부호 규약에 의존하지 않게 크기로 검사). `ChainBolt`는 뇌부 전용으로 남는다. `curse_creep_sag_px` 데이터 노브. **tools/creep_check.tscn 신설** — 전파는 저주 걸린 적이 죽을 때만 발생하는 0.45초 연출이라 일반 플레이테스트 스크린샷엔 거의 안 잡힌다(weapon_demo 6샷 전부 놓침, 연출이 안 보인 채 출고될 뻔했다). 숙주 1 + 이웃 5를 배치해 죽이고 4프레임 캡처, 실제 감염 수까지 확인(4마리). 테스트 376/376, 스크린샷 captures/n9-30/ |
| 10.99999 | ~~**N9-31 개조 게이트 첫 런 면제 제거**~~ ✅ | 완료 (2026-08-19, 오너 리포트 "낡은 부적 LV5 안됐는데도 도깨비불 얻으면 개조가 되네") — 게이트 버그가 아니라 **FTUE가 첫 런에 한해 일부러 면제**하던 것(N4-9). 그러나 그 면제는 두 번째 런부터 거짓이 되는 규칙을 가르친다 — 가르쳤다가 게임이 뒤집는 건 안 가르치느니만 못하고, 오너가 새 세이브로 첫 테스트할 때 Lv.5 지시가 안 먹는 것처럼 보이게 만들었다. 면제 없이도 교육은 유지된다: 재료 획득 시 "<무기> Lv.N에서 개조" 플로팅(_material_cue)과 일시정지 개조 경로의 "Lv.N 필요"가 **내일도 참인 규칙**을 말한다. `waive_level_gate` 인자는 레벨을 세팅하지 않고 레시피에 닿아야 하는 프로브용으로 남기되 플레이 경로에선 미사용. 새 프로필 검증: 개조 카드가 60초(보장 재료 시점)가 아니라 274초(Lv.5 도달 후)에 첫 등장. 테스트 376/376 |
| 11.0 | ~~**N9-32 필드 정돈 — 광원·테마·접지**~~ ✅ | 완료 (2026-08-19, 오너 지적 "오브젝트가 너무 많고 난잡하게 생성되어 대충 만든 것 같다") — **진단: 개수 문제가 아니었다**(화면당 솔리드 4.9 + 데코 5.9로 적은 편). 원인 3건. (1) **광원 후광이 내가 만든 회귀였다** — N10-1a에서 넣은 딱딱한 띠 5겹·알파 0.20이 불빛이 아니라 레이더 스윕으로 읽혔고 화면에서 가장 시끄러웠다 → 얇은 18스텝·알파 0.11·2.4승 감쇠의 부드러운 웅덩이로 재작업, 최외곽은 여전히 피해 반경에 정확히 일치(보이는 것 = 규칙). (2) **프롭끼리 아무 관계가 없었다** — 모든 군집이 카탈로그 전체에서 가중 추첨해서 밤 대나무숲 한복판에 대장간 모루가 서낭 옆에 놓였다 → `field.themes` 도입, 군집 하나가 **장소** 하나를 뜻하고 그 장소의 프롭만 들어간다(대숲/길가 서낭/야영터/물가). 모루는 이 스테이지 어느 테마에도 없다 — 카탈로그엔 남기되 리서치의 쇠부리터 지역에서 제자리를 찾는다. 테마 데이터가 없으면 카탈로그 전체로 폴백해 이전 동작으로 degrade. (3) **접지감 부재** — 프롭이 검은 바닥에 떠 있어 붙여넣은 것처럼 보였다 → 솔리드 프롭 밑에 충돌 박스 폭 기반 눌린 타원 그림자(코드 드로잉, 에셋 0). 데코(풀·자갈)는 눕는 물건이라 제외. validate_data 테마 계약 추가(미지 프롭 id, 양수 가중치, 각 테마가 솔리드·데코 최소 1종씩 — 없으면 그 군집의 해당 패스가 통째로 비는데 플레이에선 안 보인다). 실제 발화 확인. 미채택: 지면 텍스처 상향(적 가독성과 상충, 오너 미선택). fps 144-145 변화 없음, 승리 2/2, 테스트 376/376. 스크린샷 captures/n9-32/ |
| 11.01 | ~~**N9-49 보스 공격 패턴 2종**~~ ✅ | 완료 (2026-08-19, 오너 요청 "보스의 공격 패턴도 추가해줘 두가지 정도") — 두두리는 여태 몸으로 밀기만 했다. 데이터 구동 패턴 2종 추가: **뿌리 솟구침**(쿨 6.5s·경고 1.0s·24피해) 반경 62 원판 3개를 플레이어 **0.35초 예측 위치**에 1개 + 나머지를 110px 링으로 — 제자리는 안전하지 않고 한 걸음이 두 번 벌받지도 않는다. **목랑의 진노**(쿨 11s·경고 1.5s·30피해·넉백 2.6배) 보스 중심 반경 110~260 **띠** — 가운데를 비워서 "안으로 파고들 것인가 밖으로 뺄 것인가"의 결정이 되게 했다(원판이면 근처에 있다는 이유만으로 무는 세금). 경고 도형 `BossTelegraph`는 **한 노드가 원판·띠를 둘 다 그린다** — 보이는 것과 맞는 것이 같은 숫자에서 나와야 재조정 때도 어긋나지 않는다. 채움은 남은 시간을 읽히게 자라지만 **외곽선은 첫 프레임부터 진짜 가장자리**에 있다(채움이 따라오길 기다리면 판단이 늦는다). 스케줄링·배치·판정·검증은 순수 `BossPatterns`로 분리, 동시 만료 시 **더 오래 밀린 쪽**이 이겨 짧은 쿨다운이 긴 쪽을 굶기지 못한다. validate_data 계약: 경고 0.4초 미만 = 반응 불가라 거부, `inner_px` < `radius_px`(아니면 이름만 띠), count>1엔 양수 spread. **부수 수정**: N9-48 그슨대 어둠 소멸이 `_physics_process` 중간에 `died`를 쏴 풀로 반환된 뒤에도 `move_and_slide()`가 이어져 `body->get_space() is null` 에러 — `_update_shadow`가 자기 해제 여부를 반환하고 호출부가 즉시 중단. 검증: 실런에서 두 패턴 모두 발화(headless 로그 + 렌더 스크린샷 2장, 띠가 원판으로 보이면 실패이므로 도형별 1장씩), 테스트 388/388, validate 17파일 PASS, 런타임 ERROR 0 |
| 11.02 | ~~**N9-50 일시정지가 삼킨 손가락 — 조이스틱 고착**~~ ✅ | 완료 (2026-08-19, 오너 리포트 "이동은 정상인데 그 다음 도깨비 잡아보는거에서 드래그하면 조이스틱이 굳어서 안움직여 그 뒤로도 계속") — 재현 경로: 이동 페이지(await)는 `paused=false`라 손가락을 누른 채 220px를 걷는다 → 다음 페이지는 탭 넘김이라 `paused=true` → 손을 떼는 **release 이벤트가 일시정지된 조이스틱에 전달되지 않는다** → `_touch_index`가 잡힌 채 남아 이후 모든 터치가 거부되고, 드래그는 **옛 원점** 기준으로 계산돼 스틱이 그 자리에 굳는다. 레벨업 팝업도 트리를 멈추므로 드래그 중 팝업이 뜰 때마다 재발 — 오너가 말한 "그 뒤로도 계속"이 이것이다. 수정은 `NOTIFICATION_PAUSED`에서 스틱을 놓는 **한 곳**: 런을 얼리는 화면(가이드·레벨업·일시정지·결과)이 전부 트리 일시정지를 거치므로 호출부마다 기억할 필요가 없다. **하네스가 이 계열 버그를 두 번 놓친 이유도 같이 고쳤다** — tutorial_check가 stage를 `PROCESS_MODE_ALWAYS`인 자기 밑에 붙여서 실게임과 달리 일시정지 중에도 월드가 입력을 받았고, 게다가 액션을 전부 `notify_action`으로 위조해 **화면에 손가락을 한 번도 올리지 않았다**(N9-44도 같은 자리에서 놓쳤다). 이제 stage를 명시적으로 PAUSABLE로 두고, await 페이지마다 `Input.parse_input_event`로 진짜 press/drag를 쏘며, **방향까지** 검사한다(고착된 스틱은 이전 벡터를 계속 보고하므로 "출력이 0이 아님"은 통과해버린다). 수정 없이 실행하면 `page 3: dragged (1,0), stick reported (0,-1)`로 실패, 수정 후 통과. 유닛 테스트도 수정 없이 실패하는 것으로 확인. 테스트 389/389, validate 17파일 PASS, 런타임 ERROR 0 |
| 11.03 | ~~**N9-51 씬 하네스를 CI에 — 그리고 둘은 이미 고장나 있었다**~~ ✅ | 완료 (2026-08-19, 오너 지시 "다 개선해") — 유닛 스위트는 씬이 돌아야만 보이는 것을 못 본다(붙잡힌 튜토리얼, 삼켜진 터치, 안 나가는 투사체). 그건 tools/의 증거 하네스가 보는데 **사람이 손으로 돌릴 때만** 봤다. 그래서 점검하니 7개 중 2개가 이미 죽어 있었다. (1) **n64_check가 계속 FAIL**이었다 — 프로필을 세팅하지 않아 첫 실행 가이드가 열렸고, 가이드는 전투 페이지 전까지 무기 발사를 붙잡으므로 1초에 쏘는 이 프로브는 늘 "no projectile fired"를 봤다. N9-36 스테이지 튜토리얼이 들어온 이래 줄곧 빨간불이었고 아무도 안 봤다. 조준을 검사하는 프로브지 튜토리얼을 검사하는 게 아니므로 **되돌아온 플레이어 프로필**(메모리 전용, `_write_locked`)로 부팅. (2) **pickup_check는 헤드리스에서 영원히 멈췄다** — 렌더링 장치가 없으면 `frame_post_draw`가 오지 않는데 가드 없이 await해서, 그 아래 어서션이 아예 실행되지 못했다(다른 하네스엔 있는 가드가 빠져 있었다). CI 스텝 추가: 7종(creep·meta_flow·music·n64·pickup·shadow·tutorial)을 한 스텝에서 돌려 **하나가 빨간불이어도 나머지가 가려지지 않게** 하고, 전부 자기 어서션으로 non-zero 종료한다(실패 시 실제로 1로 나가는지 확인함). 로컬 합계 28초. `set -o pipefail` 필수 — 없으면 `if !`가 Godot이 아니라 tee의 상태를 읽어 무조건 초록불이 된다(이 저장소가 전에 겪은 그 함정). CI 루프를 그대로 로컬 실행해 초록 확인, 조이스틱 수정을 임시로 끄면 `failed:[ tutorial_check]`로 잡히는 것까지 확인. 부수: ci.yml 임포트 2회 통과가 이미 들어와 있는데 주석이 아직 "미처리"처럼 읽혀 정정(N9-26 기록의 '미처리'도 이 항목으로 해소) |
| 11.04 | ~~**N9-52 효과음 — 있던 건 버스뿐이었다**~~ ✅ | 완료 (2026-08-19, 오너 지시 "다 개선해") — Effects 버스도, 효과음 볼륨 슬라이더도, 세이브 키도 이미 있었고 **소리만 없었다**(오너가 준 오디오는 BGM 3곡뿐). 무음으로 남기는 대신 **합성**했다 — `SfxSynth`(순수 static, 노드 없음)가 레이어(파형·주파수 스윕·엔벨로프)를 믹스하고, `tools/make_sfx.gd`가 `asset/sfx/*.wav` 8종을 굽는다(hit·crit·kill·pickup·levelup·skill·hurt·boss_warn, 합계 92KB). 노이즈까지 시드 고정이라 **재생성해도 바이트 동일** — 다시 구워도 diff가 비어 자산이 바뀐 것처럼 보이지 않는다. PlaceholderArt와 같은 지위다: 실제 녹음을 같은 경로에 덮으면 코드 변경 0. 사운드 설계 의도: 타격음은 1초에 여러 번 나므로 **짧고 둔하고 낮게**(밝으면 1분 만에 피로해진다), 치명타는 **음량이 아니라 음정**으로 구분(더 큰 타격음은 그냥 소음이지만 올라가는 울림은 시선이 딴 데 있어도 "지금 그거 들어갔다"로 읽힌다), 처치는 울리지 않고 떨어진다, 레벨업만 유일하게 음악적(런에서 명백히 좋은 소식인 유일한 순간), 보스 경고는 도형이 화면 밖에서 시작할 수 있으니 반드시 들려야 한다. `SfxService` autoload: 보이스 8개 풀 + **id별 스로틀**(난전에서 같은 파형이 열 겹 쌓이면 의도보다 크고 뭉개진다, 간격은 데이터), 보이스가 다 차면 가장 오래된 것을 뺏는다(플레이어가 보고 있는 건 최신 이벤트다), 파일이 없으면 경고 1회 후 무음. 트리 일시정지 중에도 동작 — 레벨업 소리는 카드 화면이 열리는 그 프레임에 울리므로 안 그러면 영영 안 들린다. **한계 명시: 나는 이 소리들을 들을 수 없다.** CI가 검증할 수 있는 건 측정 가능한 것뿐이라 그것만 검사한다 — 길이·헤드룸(0.7 정규화, 레이어 3겹을 쌓아도 클리핑 없음)·실제 감쇠·첫 샘플이 무음에서 시작(아니면 매번 클릭)·PCM16이 32768로 감싸 돌지 않음(피크마다 클릭이 난다). 마음에 안 드는 소리가 있으면 `tools/make_sfx.gd`의 정의를 고치거나 wav를 덮으면 된다. **부수 발견·수정**: tutorial_check가 프로필을 갈아끼우면서 `_write_locked`를 안 걸어, CI에 넣은 뒤로 **개발자 디스크 세이브를 실제로 덮었다**(이 세션에서 발생 확인). 잠금 추가. 검증: music_check를 오디오 하네스로 확장해 8종 전부 Effects 버스에서 재생·루프 아님·스로틀이 반복 타격을 보이스 1개로 접는 것 확인, 테스트 401/401, validate 17파일 PASS, 실런 ERROR 0, fps 145 변화 없음 |
| 11.05 | ~~**N9-53 n64_check 안정화 — 빨간불이 진짜였다**~~ ✅ | 완료 (2026-08-19, N9-51 후속) — CI에 넣자마자 n64_check가 빨간불로 떨어졌다. 프로필 문제(N9-51)를 고친 뒤에도 **로컬 4회 중 3회 실패**. 프로브 자체에 결함 4건이 있었다. (1) **플레이어가 늘 원점에 박혀 있었다** — `bounds`가 빈 `Rect2()`(= 무제한이라는 뜻, Player는 `has_area()`로 가드한다)인데 프로브는 가드 없이 clamp해서 좌표가 항상 (0,0)이 됐다. 프롭 옆에 선 적이 한 번도 없고, 원점 사거리 안에 프롭이 우연히 있을 때만 통과했다. (2) **카메라 스무딩** — 텔레포트해도 카메라가 뒤처지는데 조준은 화면 안만 한다(N3-15). 120px 옆 프롭이 무기에겐 안 보였다. `reset_smoothing()` 추가. (3) **지표가 틀렸다** — 필드 프롭 HP 총합을 봤는데 프롭은 카메라 이동에 따라 스트리밍으로 들어오고 나간다. 총합은 피해와 무관하게 양방향으로 움직인다. 폴링마다 **직전에도 존재하던 개체의 HP가 줄었는지**로 교체 — 생성·소멸은 절대 카운트되지 않는다. (4) **예산 타이머가 12배 빨리 탔다** — 폴링은 0.05초마다 도는데 `_probe_left`를 매번 `STEP_GAP_SEC`(0.6) 깎아서, 6초 허용치가 0.5초 만에 소진됐다. 기다리는 일이 일어날 시간 자체가 없었다. 실시간(delta)으로 정정. 전방 발사 판정도 다시 썼다 — 투사체는 **풀링되므로 instance id로 신·구를 못 가르고**, "하늘이 빌 때까지 기다리기"는 기능이 정상일 때 영원히 안 비어서 교착됐다(전 런 오탐 실패). 지금은 `_velocity`(위치 차이는 갓 발사된 탄에서 0에 가까워 아무 방향이나 가리킨다)로 방향을 읽고, **facing과 맞는 발이 나올 때까지 폴링**한다 — 카메라가 자리잡는 동안 스트리밍된 프롭 한 프레임짜리를 쏘는 건 정당한 조준이지 실패가 아니다. 실패는 타임아웃뿐. 결과 8/8 연속 통과(그 전 1/4). **교훈: 이 프로브는 만들어진 뒤로 한 번도 자기가 말하는 것을 검사한 적이 없다** — 원점에 박힌 채 우연히 사거리에 든 프롭을 맞히고 초록불을 켰다. CI에 넣지 않았으면 계속 그랬을 것이다 |
| 11.06 | ~~**N9-55 필드 패시브 — 주운 것은 칸을 안 센다**~~ ✅ | 완료 (2026-08-19, 오너 지시 "맵에 패시브 스킬들이 떨어져있고, 패시브가 이미 4개 가득찼을 경우에 떨어져있는 패시브를 주우면 4개 이상으로도 등록이 되도록, 뱀파이어서바이벌 처럼") — 22초마다 최대 3개까지, 플레이어 기준 620~1100px 링 위(화면 밖)에 패시브가 놓인다. 걸어가서 주우면 **4칸 제한을 무시하고** 등록된다. 규칙의 근거: 캡은 **레벨업 화면이 강제하는 예산**이고 그건 메뉴에서 고르는 행위다. 화면 밖까지 걸어가 눈에 보이는 것을 집는 건 반대 성격의 행위라 같은 세금을 물리면 그 여정이 무의미해진다. 만렙 패시브는 후보에서 빠진다 — 아무것도 안 주는 픽업은 지키지 않는 약속이다. `KINDS`(파괴물 굴림표)에는 **일부러 넣지 않았다**: 항아리에서 패시브가 나오면 "찾아낸 것"과 공짜로 굴러오는 전리품이 구분되지 않는다. 버려진 것은 `despawn_px`로 회수 — 안 하면 3개가 뒤에 박힌 채 max_live를 잡아먹어 앞쪽 필드가 아무것도 안 내놓는다(실측 배치 4개 → 14개). 일시정지 표기는 캡을 넘으면 분수를 그만둔다: `패시브 6 (칸 4 + 주움 2)` — "6/4"는 보상이 아니라 카운터 버그로 읽힌다. **아이콘 함정**: 패시브 아이콘은 레벨업 카드용 512px 원본이라 14px 사각형에 직접 그리면 글리프가 통째로 사라지고 **단색 블록**이 나온다(오너가 전에 신고한 흰 사각형과 같은 계열). 로드 시 한 번 Lanczos로 배지 크기로 줄여 캐시 — 축소가 점 샘플링이 아니라 평균이라 모양이 살아남는다. 검증: 유닛 8종(캡 무시·만렙 제외·주석키 무시·링 배치·음수 거리·출고 데이터·화면 안 스폰 거부·블록 누락), pickup_check에 4칸을 서로 다른 패시브로 채운 뒤 5번째를 떨어뜨려 `4 taken -> 5 with 4 slots` 확인, 렌더 스크린샷으로 실제 모양 확인, 실런 ERROR 0·fps 145. **부수 수정 2건**: SFX 스트림이 전역 리소스 캐시에 남아 종료 시 `resources still in use` ERROR(→ MusicService와 같은 CACHE_MODE_IGNORE, 자체 dict가 캐시 역할), music_check의 스로틀 검사가 **레이스**였다(직전 루프가 방금 hit을 재생해 창이 닫혀 있었고, 통과하던 건 루프가 우연히 오래 걸린 덕이었다 → 창을 비우고 측정) |
| 11.07 | ~~**N9-57 화면 밖 화살표**~~ ✅ | 완료 (2026-08-19, 오너 지시 "무언가가 있다는 화살표 표시나 이런게 필요할거고") — N9-55가 필드 패시브를 **일부러 화면 밖에** 놓으므로(걷는 것이 요점), 표시가 없으면 존재 자체를 모른다 — 보이지 않는 보상은 없는 보상과 같다. 화면 밖 드랍마다 가장 가까운 가장자리에 금색 화살표를 고정한다. **배치 방식**: 타겟 위치를 그대로 clamp하지 않고 **중심에서 타겟 방향으로 나가며 먼저 닿는 경계에서 멈춘다** — clamp는 플레이어가 움직일 때 화살표가 가장자리를 미끄러져 방향이 아니라 떨림으로 읽힌다. 경계는 HUD를 피해 잡는다(아래 96px = 액티브 버튼, 위 74px = 타이마·바) — 누를 버튼 위에 걱치면 표시가 조작을 방해한다. 거리는 투명도로 전달하되 **0으로는 안 내려간다**(0.35 바닥) — 지워지면 먼 것이 아니라 렌더링 글리치로 보인다. 입력은 절대 먹지 않는다(`MOUSE_FILTER_IGNORE`) — 조이스틱 위에 격는 레이어다. 검증: 유닛 10종(화면 안은 미표시·경계 정확도·각도·대각선이 가까운 변을 택하는지·32방향 전부 경계 안·HUD 회피·크기 0 화면·페이드 단조성·정중앙 예외), pickup_check에 먼 드랍을 하나 더 놓아 **배지와 화살표를 한 프레임에** 캡처(captures/n9-55/), 테스트 419/419, 하네스 7종 전부 초록, 실런 ERROR 0 |
| 11.08 | ~~**N9-58 해금 시스템 + 해금 화면**~~ ✅ | 완료 (2026-08-19, 오너 지시 "지도같은건 해금해야 얻을 수 있도록 해금 기능도 활성화 하자") — 계정이 계속 산다는 영구 해금을 골드로 구매하는 체계. **명부수 트리와 분리한 이유**: 트리는 스탯 랭크 전용이고 모든 노드가 실제로 소비되는 스탯을 먹여야 한다는 규칙을 MetaTree가 강제한다. 해금은 숫자가 아니라 '가졌다/없다'라, 트리에 넣으려면 **해금마다 가짜 스탯을 만들고** 그 규칙을 느슨하게 해야 한다. 프로필엔 id만 남기고 가격·이름은 데이터에 두어 **가격 조정이 세이브를 건드리지 않는다**. 구매는 전부 되거나 전부 안 되거나다 — 거절되면 원본 프로필을 그대로 돌려줘 골드만 빠지는 상태가 불가능하다. 화면은 **누르기 전에** 가격과 구매 가능 여부를 말한다(누르게 해놓고 안 된다고 설명하는 패턴은 이 프로젝트가 계속 되돌리는 것이다), 보유한 줄은 아예 비활성화된다. 첫 항목은 **지도 900냥**(N9-59가 사용). 검증: 유닛 13종(정확한 금액 경계·중복 구매 거절·미지 id 거절·거절 시 프로필 불변·삭제된 id 정리·로케일 폴백·세이브 강제변환), 신규 `unlocks_check` 하네스가 실제로 사서 골드가 가격만큼 움직이고 보유 줄이 다시 눌리지 않는지 확인(CI 포함), 스크린샷 2장. **부수 수정**: `flat = true` 버튼은 스타일박스를 아예 건너뛰어 카드가 배경 없는 글자로만 떠 있었다(스크린샷으로 발견), 쓰기 잠금 경고가 하네스에서도 "더 새 빌드의 프로필"이라고 거짓말하던 것을 이유별로 분리 |
| 11.09 | ~~**N9-59 지도 (해금 대상)**~~ ✅ | 완료 (2026-08-19, 오너 지시 "위치를 보기위한 지도도 필요할건데 지도같은건 해금해야 얻을 수 있도록") — N9-58 해금을 보유했을 때만 좌상단에 원형 지도가 그려진다. **지도가 아니라 레이더다** — 지형은 안 그린다. 필드는 절차적으로 뿌려지므로 대나무 그림을 그려봐야 창밖으로 이미 보이는 것을 반복할 뿐이고, 안 보이는 건 **떨어진 물건의 위치**라 그것만 찍는다(전리품·상자·필드 패시브). 반경 1250px는 **필드 패시브의 최대 생성 거리(1100px)보다 넓게** 잡았다 — 지도가 거기 못 닿으면 보여주려는 바로 그 물건이 안 보인다(테스트로 강제). 반경 밖은 버리지 않고 테두리로 끌어 **속이 빈 점**으로 그린다 — 사라지면 없는 것과 같고, 가끔 채우면 "바로 여기"로 읽힌다. 경계값은 안쪽 소속이라 플레이어가 그 주위를 돌 때 점이 깜빡이지 않는다. 위치는 좌상단 — 위는 타이마·바, 오른족 아래는 액티브, 아래는 엄지라 지도가 아무것도 건드리지 않는 유일한 구석이다. 갱신은 5Hz(매 프레임 자식 순회는 낭비). 검증: 유닛 8종(중심·방위 보존·거리 비례·림 클램프·64방향 전부 원안·경계값·0 반경·스폰 링 커버리지), pickup_check가 **해금 전엔 노드 자체가 없는지**까지 확인한 뒤 사서 보유 상태로 다시 확인("떴다"와 "올바른 이유로 떴다"는 다른 주장이다), 테스트 440/440, 하네스 8종 초록, 실런 ERROR 0·fps 144. 부수: 프로브가 골드 부족으로 구매 거절당해 **조용히 통과하던 것**을 발견해 자금을 먼저 넣고 거절 시 실패하도록 고쳤다 |
| 11.10 | ~~**N9-60 무한 모드 (끝없는 밤)**~~ ✅ | 완료 (2026-08-19, 오너 지시 "무한모드도 존재했으면 좋겠어") — 시계가 없는 런. 본거지 출정 줄의 **길이** 선택지에 `끝없는 밤`이 추가된다(기존 단축·표준·긴 밤과 같은 자리, 화면 작업 0 — 카망 화면이 run_lengths를 그대로 순회하기 때문). **새 웨이브 표를 만들지 않고 스테이지 자신의 웨이브를 재생한다** — 둘 중 하나만 조정되는 순간 어긋나기 때문이고, 그래서 본편을 다듬으면 무한 모드도 같이 다듬어진다. 120초 지점부터의 웨이브를 180초마다 되풀며 루프당 수 +25%·HP +45%·피해 +30%. **도입부(0초 러시)는 일부러 뺀다** — 순한 도입부를 매 루프 다시 틀어주면 밤이 갈수록 **쉬워진다**. 증가는 지수가 아니라 **선형** — 지수는 몇 루프 만에 불가능해지고, 무한 런의 재미는 빌드와 압박이 자리를 바꾸는 긴 중반에 있다. 보스는 **결승선이 아니라 반복 이벤트**가 된다 — 잡으면 보상을 주고 220초 뒤에 다시 온다(루프 배수가 보스에도 걸려서 돌아올 때마다 세진다). `duration_scale: 0` 같은 매직 값을 쓰지 않고 명시적 `endless` 플래그를 둔 이유: 검증기가 모든 항목에 양수 scale을 요구하므로 0을 허용하면 '무한'과 '설정 오류'가 같은 값이 된다. 검증: 유닛 15종(루프 경계·선형 증가·도입부 미재생·재타임·수 증가·수 0 방지·루프 0 미재생·침묵하는 루프 거부·성장치 누락 거부·출고 데이터 정합), 신규 `endless_check` 하네스가 무적 플레이어로 560초까지 돌려 **시계가 안 울리고(outcome 공백), 루프 3회 장전, 보스 2회 처치, 필드 정적 0초** 확인(CI 포함), 카망 스크린샷으로 선택 가능함 확인, 테스트 455/455, 하네스 9종 초록, 일반 런 ERROR 0 |
| 11.11 | ~~**N9-61 도달 검사 + 누락된 .uid**~~ ✅ | 완료 (2026-08-20, 오너 지시 "제대로 다시 점검해서 진행해") — 직전 사고의 재발 방지. `git add` 경로 목록에 없는 파일이 섞여 명령 전체가 실패했는데 확인 없이 커밋해, **해금 화면과 데이터는 있고 거기로 가는 본거지 버튼은 없는** 상태가 main에 올라갔다. 모든 테스트가 초록이었다 — 어느 검사도 '화면에 들어갈 길이 있는가'를 묻지 않았기 때문이다. 신규 `nav_check`: 본거지가 제공하는 건물마다 **준비됨 → 씨이 존재·인스턴스화·실제로 내용을 그리는가**, **준비 중 → 공지를 말하고 어디로도 안 가는가**를 검사한다. `change_scene_to_file`로 이동하지 않고 인스턴스화하는 이유: 이동하면 하네스 자신이 교체되어 런이 끝난다. 그래도 `_ready`는 실행되므로 누락된 로케일 키나 잘못된 노드 경로는 거기서 터진다. **검사가 실제로 잡는지 증명했다** — 라우팅을 임시로 빼자 `해금 is ready but routes nowhere`로 실패. 부수: 신규 스크립트 8개의 `.uid`가 미추적이었다(저장소는 123개를 추적 중이므로 정책 위반) — 함께 커밋. 검증: **깨끗한 클론**을 띄워 콜드 임포트(오류 0)·테스트 455/455·데이터 18파일·하네스 9종 전부 통과 확인(내 작업 트리의 미추적 파일이나 따뜻한 캐시에 기대지 않음을 입증), 그 뒤 nav_check 포함 10종 초록. 미해결 보고: 저장소 루트의 `tmp/`(44파일, 8/18 에셋 작업 잔재, 코드 참조 0) — 오너 파일이라 지우지 않고 보고만 한다 |
| — | **콘텐츠 트랙 (조선 특색)** — 소재는 [docs/JOSEON_RESEARCH.md](docs/JOSEON_RESEARCH.md) | 전략 감사 2026-08-19: 코어 루프는 돌아간다, 부족한 건 콘텐츠 물량(플레이어블 1·지역 1·몬스터 5/23·해금 0) |
| C1a | ~~**N10-1a 그슨대 — 빛 안에서만 벨 수 있는 괴이**~~ ✅ | 완료 (2026-08-19) — 첫 "때려 죽인다를 깨는" 괴이. 어둠에서는 모든 피해를 흡수하고(투사체·원호·장판·화상/저주 DoT 전부 `Enemy.take_damage` 단일 관문에서 차단 — 무기별 특례 0) 시간에 따라 커지며 접촉 피해가 함께 오른다. 광원 안에서만 피격 가능해지고 크기가 되돌아 줄어든다. 광원은 기존 프롭 재사용(석등 170px·모닥불 200px `light_radius_px`, 석등 weight 0.5→1.5 — 이제 규칙을 지닌 프롭이라 풍경 밀도로는 부족), 무한 맵 청크가 추가한 광원도 같은 배열 참조로 즉시 반영. **어둠 속 그슨대는 조준 대상에서도 빠진다**(`Spawner.active_enemies` 단일 필터, 그슨대가 없는 런은 `_active`를 그대로 반환해 할당 0) — 안 그러면 자동 조준이 못 죽이는 적에 묶여 죽는다. 광원 웅덩이를 실제로 그려서(LightHalo, 1회 draw) 어디가 빛인지 보이게 했고, 등장 시 1회 "빛 안에서만 벨 수 있다" 안내. **구조 수정 1건**: 무제한 추격형으로 만들었더니 봇이 3시드 전패(184.7s/260.4s) — 못 죽이는 놈이 끝까지 쫓아오면 수명 타이머일 뿐이라 수치 완화로는 형태가 안 고쳐진다. `leash_px` 420으로 자기 어둠을 지키게 바꿔 해결(플레이어 이속 90 vs 36, 걸어서 벗어날 수 있음). 측정: 리시 이후 6런 중 승리 4(382.9-395.3s, 기존 기준선 379.9s와 동일 대역)·패배 2(그슨대 289.9s, 죽림 거한 315.3s — 프로젝트 표준 가드 스프레드 내), 서지 fps 변화 없음. validate_data가 계약 강제(shadow 수치 양수, max_scale>1, 광원 프롭 0개면 무적 몬스터라 FAIL — 실제로 발화 확인). tools/shadow_check.tscn 런타임 증명(어둠 흡수/성장/조준 제외 → 광원 내 피격/축소/처치, 필드 시드 무작위라 어두운 지점을 탐색해서 사용), 테스트 349/349, 스크린샷 captures/n10-1a/ |
| C1 | **N10-1 괴이 4종 — 루프를 깨는 나머지** | 야광귀(이속 아이템 절도, 체 오브젝트로 정지) · 삼두구미(무쇠·생달걀·버드나무 3약점 부위 파괴) · 영노(오브젝트·투사체를 먹어 100스택 = 플레이어 패배, DPS 체크) · 손님/호구별성(때리면 강해지는 보스, 헌납 3회 + 배웅 호위로만 퇴장). 전부 "때려 죽인다"를 한 번씩 깬다 — 이게 채택 이유다. **한 마리 = 한 피처로 분해할 것.** 실록 계열 아님(방언·무가·제주 구전), 원문 대조 불필요 |
| C2 | **N10-2 나장/포졸 플레이어블** | 직업 12종 중 엔진 추가 작업 최소. 자원 없음(쿨다운만) = 입문 난이도 최저, 육모방망이 3타째 기절 + 오랏줄 결박(끌어당김 + 받는 피해 +50%). 딜 최하·CC 최고로 도사와 축이 안 겹친다. **게이트 확인 필요**: 도사 완성 조건(2026-08-14 오너 지시)이 충족됐는지 먼저 판정 |
| C3 | **N10-3 여단(厲壇) 지역** | 1401년 권근 주청으로 북교에 세운 무주고혼 제사 시설. **제사 게이지 — 여귀는 무한 스폰이고 죽여도 안 줄어들며, 제물을 여단에 바쳐야 일제히 성불한다.** 킬 플레이가 손해가 되는 유일한 스테이지이고, 조선 국가 의례가 그대로 게임 규칙이 되는 유일한 사례다. 기존 공동묘지 계획(N8-2)보다 정체성이 강함 — 순서 재검토 |
| C4 | **N10-4 해금 목록 화면** | 감사 지적: 해금 콘텐츠 0개. 무엇을 향해 달리는지 보이는 화면이 없으면 사다리도 괴이도 동기가 안 된다 |
| 11 | **N9-1 사운드** | BGM + 효과음 |
| — | **게이트 통과 후** | |
| 12 | N8-1 두 번째 캐릭터 | 도사 완성 확인 후 |
| 13 | N8-2 두 번째 지역 (공동묘지) | 도사 완성 확인 후 |
| 14 | RZ-1 안드로이드 실기기 검증 | |

큐 밖(수시): 오너가 `new_asset/`에 에셋 떨구면 가공·연결 커밋 1건.

## AC-트랙 — 에셋 (codex, 별도 커밋)

- [x] **AC-1 도사 캐릭터 세트** — 인월드 idle+walk4 + 선택 카드 초상
      (오너 제공 시트 가공)
- [x] AC-2 타이틀 아트 (밤하늘/한옥 마을/현판 로고 ko·en)
- [x] AC-3 UI 크롬 (나무 버튼 9-slice/창살 코너/무기·전리품 아이콘) —
      아트 커밋(009e7b0) + N3-13 연결로 완료
- [x] AC-4 전장 타일/프롭 (대나무숲 야간, 조용한 바닥 타일 재생성 포함)
- [x] AC-5 몬스터 1지역분 (도깨비/원혼/거한/정령왕)

---

## Done log

| Date | Feature | Commit |
|---|---|---|
| 2026-08-20 | N9-79 chain weapons stop carrying travel art (owner: 뇌부는 애초에 투사체가 아니라 번개로 하기로 했잖아): 뇌부 · 뇌정부 declared `travel_sprite` while AutoWeapon resolves a chain weapon on the enemy it aimed at (`strike_now`) and Projectile returns on its first physics frame (`_instant`) — the sprite was never a flight, and ChainBolt already draws the jump. Both keys dropped and both PNGs deleted (N9-78 had just installed new art into files nobody renders), asset/build_from_generated.py entries removed, and validate_data now FAILS any weapon with `mechanic: chain` that declares `travel_sprite` — proven by re-adding one and watching it fail. tests 484/484, validate_data PASS | — |
| 2026-08-20 | N9-78 Joseon weapon art (owner: 법검이 서양 브로드소드, 봉인진언이 자물쇠): 21 generated images cut and installed at their real paths by asset/build_from_generated.py — 9 weapon icons (낡은부적 · 화염부적 · 법검 · 봉마검 · 화부 · 뇌부 · 뇌정부 · 진언 · 봉인진언), 10 projectiles, 신장 도형 + 석장 원호. The installer reads every target size off the file it replaces, so an art swap can never quietly change how big something draws. Two rounds were needed: round one put a rock platform under half the projectiles, covered every talisman in invented letterforms, turned both slash trails into crescent MOONS, and — my error, caught by the owner — redrew 법검/봉마검 projectiles as whole flying swords when the shipped design is a 20x7 sliver of blade energy that projectile.gd rotates to the travel direction. hit_phoenix (5 frames) and swing_arc (2 frames) are strips, so a still cannot replace them; both are logged in ASSET_REQUIREMENTS.md with their reference art. tests 484/484, validate_data PASS, weapon_demo on 법검/뇌부/신장 with no errors | — |
| 2026-08-20 | N9-77 owner monster art integration (도깨비 · 숲 정령 · 죽림 거한 · 두두리): four 4x2 8-frame drops in new_asset/owner/monster wired through a shared asset/monsters/build_owner_sheets.py, replacing the per-monster copies of the 그슨대 pipeline (row-local alpha bounds so neither row loses its feet, premultiplied BOX down to logical then NEAREST back up to 16x, standing frame picked by narrowest stance, boss idle_breathe from that frame and its neighbour). The build scales each figure to a DECLARED drawn height rather than letting it fill the frame: filling it made the goblin 32px against the taoist's 38 and put the spirit below the goblin, breaking the on-screen threat hierarchy the tests assert. Measured 27 / 29 / 45 / 84 (goblin < spirit < player < brute << boss). Two consequences reported rather than hidden — the new goblin is a slimmer drawing, so its hurt circle drops 9.5→8.5 to keep "what you see is what hits", and the brute's recorded height moves 44→45; tests 484/484, validate_data PASS, rendered playtest to 5:10 with no script errors | — |
| 2026-08-20 | N9-76 수행자 선택 재배치 (owner: 상단 확대 이미지 + 설명 + 해금 방법, 하단 작은 타일 정렬): full-width row cards replaced by one detail panel (196px portrait well, name+hanja, 칭호, scrolling backstory) over a horizontally scrolling tile strip, so the screen holds its shape as the roster grows instead of running off the bottom at four characters; viewing and selecting split — a locked tile MOVES the panel (its unlock condition under a 해금 방법 heading) but never the saved selection, proven by tools/select_check.tscn which was made to FAIL by reverting `view` to reject locked ids; hanja stand-in in the portrait well where art is missing (warrior/archer); `card_model` gains `description` (backstory, quote fallback); fixed a latent crash carried over from the row cards — the rebuild ran from a tile's own `pressed` signal and freed the emitting button (now detach + queue_free, which also frees the node names); tests 484/484, validate_data PASS, nav_check PASS | — |
| 2026-08-14 | Full rebuild decision; docs reset (DESIGN v3 / ROADMAP / TASKS) | — |
| 2026-08-14 | N0-1 repo reset (old code/assets removed, font kept) | 7042085 |
| 2026-08-14 | N0-2 minimal boot: title stub + test runner + data validator | — |
| 2026-08-14 | N1-2 title art integration: sky/village texture layers + locale-swapped signboard logo | — |
| 2026-08-14 | N1-1 design tokens (UiPalette/UiLocale/WoodButton, global theme) + title layout | — |
| 2026-08-14 | N3-1 stage scene: camera-follow player, touch joystick + WASD, title routing | — |
| 2026-08-14 | N3-4 enemy spawn/chase/contact damage/death: pooled wave spawner, player HP + invuln, data cross-checks | — |
| 2026-08-14 | N3-3 + N3-5 auto-attack talisman, damage numbers, XP orbs + magnet, RunState leveling (progression.json curve) | — |
| 2026-08-14 | N3-6 power-up popup: paper panel row cards, real-number descriptions, weapon levels + passive stacks, queued multi-level | — |
| 2026-08-14 | N3-2 taoist in-world sprite: idle + 4-frame walk (8fps, speed-scaled), facing mirror, 16x NEAREST downscale | — |
| 2026-08-14 | N3-7 combat HUD: outlined timer, thin XP bar, Lv/kill/gold counters, glyph placeholder icons, paper-panel pause overlay, minimal player HP bar | — |
| 2026-08-14 | N3-9 procedural bamboo forest field: data/props.json catalogue, seeded deterministic scatter, solid StaticBody2D props, enemy slide/avoid steering, Y-sorted world | — |
| 2026-08-14 | N3-8 hit feedback: white flash + knockback (data/effects.json), pooled particle-free death puff, player screen-edge vermilion vignette | — |
| 2026-08-14 | N5-1a boss wave: data-driven boss_at_sec spawn, no-despawn/no-cap boss, top thin HP bar, gold boss damage numbers | — |
| 2026-08-14 | N5-1b run result screen: RunFlow outcome arbiter (death > boss kill > timeout-as-victory), paper-panel 승리/패배 + time/kills/gold, CTA to title | — |
| 2026-08-14 | N3-10 bamboo forest art integration: real tile/prop/decor textures, seeded ground-variant tiling (GroundLayer), base-footprint collision boxes rebuilt from delivered art | — |
| 2026-08-14 | N4-1 loot drops + magnet pickup + special-material 3-choice popup: seeded drop rolls, tier-tinted pooled diamonds via XP-orb magnet path, RunState run inventory, use/keep/salvage cards on the shared paper-panel component, weapon-mod swap carrying level, tinted modded projectiles, loot data cross-checks | — |
| 2026-08-14 | N3-12 monster/boss sprite wiring: data-driven sprite sets (idle + 4-frame walk, boss idle_breathe), shared SpriteSheet builder with the player, facing mirror + overbright hit flash, footprint-based hurt circles, validate_data sprite-file check | — |
| 2026-08-14 | N3-11 fix: prop render scale now derived from visible silhouette content (not the padded export canvas) so declared logical heights actually render; ground variants placed as sparse noise-clustered patches with per-tile rotation instead of an even scatter | — |
| 2026-08-14 | N2-1 수행자 선택 screen: full-width row cards from characters.json (accent name + hanja, 칭호, quoted line, NEAREST portrait well), GOLD-border 선택됨 badge, silhouetted locked cards with unlock text, SaveService-persisted selection consumed by the run, title corner-utility entry, card copy/accent/unlock cross-checks in validate_data | — |
| 2026-08-14 | N4-2 weapon grades + 15-min pacing: `_grades` ladder/step multipliers in weapons.json, grade-up level-up card + mod grade carry + top-grade gold callout, 15-min bamboo_forest wave curve (elite from 5:00, surge peak 840s, boss 900s, soft enrage 920s+), `bamboo_brute_elite` data-derived elite + rare-material drop table, RunFlow schedule invariants, validate_data grade/elite/pacing cross-checks, boss hp 7000 | — |
| 2026-08-14 | N4-2b 5-minute run rescale: 300s bamboo_forest curve (elite 2:00, surge peak 57 at 3:30, boss 4:00 hp 2400, enrage 4:40), XP curve 6×1.5^(L-1) for 8–10 level-ups, early special-material drop bias, duration_sec bound invariant in schedule_issues, autoplay verification harness (tools/playtest.tscn) | — |
| 2026-08-14 | N5-2 autosave/auto-resume + settings popup: SaveProfile pure helpers + SaveService autoload (user://profile.save JSON, temp-rename safe write with crash recovery, schema v1 migration hook, corrupt→warning+fresh), run-end gold banking + lifetime stats, result-screen 보유 엽전 row, paper-panel settings popup (3 volume sliders + 한국어/English toggle, applies live + persists), Master/Music/Effects buses, single-primary-action title | — |
| 2026-08-14 | N3-15 on-screen-only targeting: nearest enemy inside view rect + data margin AND per-weapon range_px (weapons.json `_targeting` + every weapon), hold-fire when nothing visible, projectile expires on leaving view rect + margin, validate_data targeting contract | — |
| 2026-08-14 | N3-14 enemy separation steering: bucketed-grid neighbour lookup (Separation, reused buffers, no per-frame alloc), weighted push blended into chase, boss push-exempt but crowd-blocking, tuning in effects.json, playtest crowd metric avg-stacked 8.46 → 0.00 | — |
| 2026-08-14 | N4-4b taoist extended kit: four new data-driven mechanics (결계 ward DoT+slow, 신장 autonomous prop-colliding summon, 진언 periodic knockback+stun shockwave, 살 death-spreading curse), four mod branches (화염 결계/뇌정 신장/봉인 진언/귀살), two actives (축지 blink+invuln, 벽사진 emergency burst) on a bottom-right HUD cluster, pooled ward/summon/pulse nodes, validate_data + pure-helper test coverage, playtest --grant surge load test 59fps | — |
| 2026-08-14 | N3-16 ground coverage fix: GroundLayer follows the camera's tile window (per-tile seed-hash variant/rotation, redraw only on window change), grey band gone at field edges, coverage invariant test + tools/ground_check.tscn edge screenshot | — |
| 2026-08-14 | N4-6 loot auto-collect + mod-as-level-up-card: special-material popup and queueing deleted (DESIGN.md §5.2), every material collects silently via the magnet path (floating cue label for specials), dead materials auto-salvage to gold at pickup, 개조 card in the level-up 3-pick (max one per screen, result grade pill, real numbers, consumes material, carries level+grade), replaced weapons permanently excluded from new/upgrade pools (owner-reported regression, covered by failing-first test), dead-inventory sweep after each mod | — |
| 2026-08-15 | N4-3 measured balance pass: playtest forced-build/batch/seed/speed harness + damage accounting, 10-weapon sweeps (BEFORE 35x → AFTER 2.3x on 3-seed averages, tables in data/BALANCE.md), 혼불 orbit-ring rework (data orb_radius_px, ring 105px), 진언/신장/결계/석장/화부/법검/살/낡은부적 retunes, new data knobs explosion.edge_falloff + pierce_retention (validate_data + unit tests), surge ranged 14→18 + escort 8→12 + enrage speed 1.45 so a pure-evasion no-pick build can die (seed99 death measured), rendered 6-weapon heavy build surge fps min 59 | — |
| 2026-08-17 | N4-8 weapon growth curves: 21 spiritual weapons reshaped (level-1 bases cut to ~70-77%, per-level steps +25-40%/level, `milestones` schema with additive mechanic deltas merged by LevelUp.stats_at_level — extra projectiles/chain jumps/arc width/orbit orbs/ward uptime/curse spread, ★ marker on milestone cards), multishot fan hook (WeaponMath.fan_directions + _targeting.multishot_spread_deg), AutoWeapon recomputes mechanic blocks per level, spawner recycles farthest offscreen enemy when live_cap blocks a due wave (zero-dps evasion runs froze the whole wave schedule), enrage 250s/1.6x + stretched escort, validate_data milestone contract, headroom x2.1-3.9 → x3.9-15.6, no-upgrade dies 3/3 (216-256s), normal wins (38-41 dps), L8 spread 2.1x, maxed-meta guard holds (1/8 nopick deaths, watch item renewed), tests 294/294 | — |
| 2026-08-15 | N4-7 mod-result weapon gating + level-up dedupe: validate_data fails any weapon_mods result missing `evolution_only`, LevelUp.pick/assemble guarantee one weapon id per screen (mod card's base+result seed the exclusion set), owned-weapon strip wraps via HFlowContainer with Lv badges overlaid on the wells (fits 540px at max owned count, layout test), playtest instrumented: 0 mod results offered as new weapons | — |
| 2026-08-15 | N5-3 base camp minimal: pure Camp helpers (post-title / select-exit routing off Ftue.is_first_run, profile summary view-model, GDD §24 building roster with 준비 중 resolution), camp scene on the dark meta grammar (GOLD title + coin/gold header, NIGHT_BROWN lifetime-stats card, 4 tappable building spots answering 준비 중, 수행자 선택 + one-tap 출정 wood buttons), title start routes returning profiles to camp (first boot still straight to stage), result CTA 본거지로 lands in camp, character-select back returns to camp for returning profiles, TEXT_MUTED_ON_DARK token, tools/camp_check.tscn screenshot harness, camp_backdrop registered in ASSET_REQUIREMENTS.md | — |
| 2026-08-15 | N7-1 명부수 meta tree: data/meta_tree.json 8-node DAG (per-rank cost ladders, prerequisites, per-stat caps), pure MetaTree helpers (purchase as one atomic profile fold, corrupt-state sanitize, capped aggregate, Kahn cycle/reachability validation), profile schema v2 with future-schema read-only fail-safe + MAX_GOLD overflow clamp, run-start single-point application into weapon/speed/magnet/hp scalars, 명부수 screen on the `_02` meta grammar (scrollable trunk graph, next-rank detail card, wood CTA hidden at max, empty-tap deselect), camp 명부수 spot routing, validate_data meta contract, playtest --meta=max + meta_flow_check persistence harness, maxed profile still loses (seed7 normal 224s / seed123 nopick 200s defeats) | — |
| 2026-08-15 | N5-4 괴이록 bestiary: pure Bestiary helpers (idempotent order-independent discovery fold persisted at sighting time so the record survives defeat and crash, unseen counter, unknown-id prune with warning, data-derived rosters — stage-spawn monsters + boss, loot.json, reachable-weapon closure over runtime_can_fire + mod chains — and ???-masked view-model rows), profile bestiary block on schema v2 with record-less migration fill, stage discovery hooks (kill/pickup/weapon-own incl. mod results), camp 괴이록 spot routes to a 3-tab row-card screen (per-section + overall counts derived from data, silhouetted ??? rows, tier/boss pills, mechanic one-liners, recipe ends masked per own discovery), one-line camp hint cleared on open, tools/bestiary_check.tscn 3-tab shots, tests 259/259 | — |
| 2026-08-16 | N7-2 meta economy rework + per-character branches: 22-node tree (13 shared trunk + 5 taoist 술법 + 2 warrior + 2 archer locked-visible with unlock text), taoist-relevant total 1,330 → 14,990냥 (~35-40 runs at the measured 83냥 loss / 299-512냥 win income, first rank affordable after one losing run), strictly-increasing per-rank cost curve + prereq-cost rule + per-character cap-waste guard enforced by MetaTree.data_issues, new wired node kinds (gold_gain/xp_gain multipliers, start_level with immediate power-up, choice_count 4-card screens, first_find guaranteed special material, damage_reduction, hit_invuln, once-per-run revive with float label), taoist branch folds into weapon data via MetaTree.modified_weapon_stats (burn duration/chain jumps/ward radius/orbit orbs/seal burst_at floor 2), per-character aggregate no-leak (unit-tested), pill-tab screen (trunk + branch per roster entry, selection follows tabs), stale-node prune on load without refund (deliberate, documented), survivability trimmed after a 6/6 unlosable nopick sweep (DR 5%/rank cap 14%, i-frames 10%/rank cap 20%, revive 30%) → maxed nopick 3W/1L (seed 5 defeat 279.5s), measured branch effect noebu chain 3→5 jumps / 22.2→32.4 dps, tests 285/285 | — |
| 2026-08-16 | N6-2 dangerous opening + legible death + HUD HP: front-loaded goblin rush (0s 14@0.7s + 12s 10@0.8s vs the old 5@2.5s) with a stages.json `opening` contract (≥18 spawns in 20s, level-2 XP scheduled by 40s, guarantee drop offset 160px ahead of travel) enforced by RunFlow.opening_issues in validate_data, killer attribution (Enemy.name_ko → Player.last_hit_source → 죽음 row on defeat, elite/boss names pass through, neutral fallback for unattributable), low-HP looping vignette + vermilion HUD fill at the data threshold (25%, 0.9s pulse), HUD HP bar under the XP bar (under-sprite bar kept), playtest --idle probe + first-level-up/0-20s-damage report (normal bot: 33.9s first level-up, 0 dmg, victory; idle bot: 30 dmg, dead 33.2s to 숲 도깨비), tests 275/275 | — |
| 2026-08-16 | QA-2 new-player experience audit + .import policy (D6): untracked all generated .import sidecars (font whitelist kept for its antialiasing=0 pixel params, fresh-clone import + 259/259 tests verified, CI cache key rehashed over asset sources, docs/CI.md updated), full first-session playthrough on a deleted save (title→first run→level-up→mod→death→camp→tree→bestiary→second run, screenshots), 3 small fixes (dead HUD info button hidden, px units stripped from card copy, meta-tree CTA disabled style when unaffordable), 9 queue candidates led by the threat-free first 90 seconds and unexplained deaths | — |
| 2026-08-16 | QA-1 pre-content full functional audit: every built feature exercised from a deleted save and a returning save (rendered autoplay runs, 10-weapon headless batch, real-save meta purchase/relaunch phases, corrupt-save boot, all screenshot harnesses), 3 fixes (water_puddle walkable decor, boss HP bar hidden on run end, playtest boss-report misprint), 4 queue-candidate reports (partial localization, no settings access from camp, .import sidecar policy split, _end_run null-guard inconsistency), tests 259/259 + validate_data PASS | — |
| 2026-08-15 | N6-1 FTUE first sixty seconds: pure Ftue helpers (first-boot taoist routing, one-shot move-hint / mod-explain profile flags, first-run guarantee table), profile `ftue` block with migration fill, MoveHint chevron overlay dismissed by first input, one extra 개조-card line "재료로 무기를 바꾼다" once per profile, stages.json `first_run_drops` (부적지 10s, 도깨비불 60s) validated end-to-end, playtest hint shot + first-run report, zero added taps | — |
| 2026-08-17 | N5-5 destructibles + pickups + elite chests: 3 breakable props (data hp, solid-only, validator), player-weapon-only damage via the spawner breakable registry (projectile flight / arc / orbit / shockwave hooks), pooled shatter puff + data break table (nothing/gold/health/nuke/magnet, plain-share ≥0.8 enforced), pickups as magnet-path world entities (full-HP health → gold at collection, nuke 999 trash / 150 elite-boss cap with one-shot validator guard, magnet attract_now full-field pull), elite is_elite flag → pooled walk-over chest, 1/3/5 rewards via luck-bent weights (validator: strict escalation + <50% five-count at luck cap), rewards drawn per-screen through the LevelUp pool machinery (1 tap per reward, fallback gold on dry pool, post-outcome discard), pickup_check screenshot harness, playtest break/chest metrics, tests 310/310 | — |
| 2026-08-18 | N6-5 gold sources + joystick options + field declutter + asset wiring: monster gold_drop removed from all 23 entries (kills pay XP + loot only) with a validator rule FAILing any resurrection, boss 150 rehomed to stages.json boss_gold paid on the kill (run ends in the same call — a chest could never open), remaining sources raised (break gold 12→40, full-HP conversion 10→25, chest fallback 40→60, salvage 5/10/18/32) and meta tree scaled to ~47% (taoist-relevant 14,990→7,945냥) → measured blended ~163냥/run, ~35-49 runs-to-max (target 30-50); joystick_opacity setting (4th slider, persisted, live at draw, clamp [0.2,1.0] default 1.0) + neutral JOYSTICK_BASE/RING/KNOB tokens replacing the wood accent; field decluttered into 9 seeded cluster discs (solids 60→40, decor 110→48, r170/gap420, reproducibility + spawn-clear + min-gap kept, new cluster test), ground variants 10-20%→4-5% at 0.55 alpha blend, breaks/run 16→mean 2.7 (0-9); pickups wired to real art (HUD coin reuse, UI-pack-3 heart, Trap-and-Weapon bomb via asset/pickups/build_assets.py night tint), magnet/chest/sinjang registered as missing (chest = headline priority), 2 UNCONFIRMED licence rows, tests 329/329, screenshots captures/n6-5/ | — |
| 2026-08-18 | N6-4 grace withdrawal + floating joystick + no-hold-fire (owner direction): popup grace removed (popup_grace_sec data + Stage grant + validator field; timed-shield plumbing kept for 축지/회생부, tests relabeled), soft_enrage.damage_mult_max 2.8→2.5 reverted and re-measured (metamax nopick 1 defeat/6 seeds at 279.5s — pre-grace guard standard restored; plain nopick 219.1s defeat, idle dead 32.8s, normal victory), rush/level-up separation via wave2 12s 10@0.8 → 17s 10@0.7 (rendered 1x: near-at-popup-close 11→9, wave-2 ring lands ~5.5s after the close, opening validator 19≥18 spawns, 0:10 tension untouched, idle 0-30s damage 108 — still lethal), floating joystick (touch anywhere anchors stick, resting pad kept as FTUE hint anchor, CombatHud.blocks_point tap priority, second-finger safe, WASD unchanged, 5 pure-math tests), uniform targeting fallback CombatMath.fallback_aim enemy→destructible→facing for projectiles/arc/ward with zero per-weapon cases (5 tests + n64_check runtime proof: prop hp 1414→1403 with zero enemies, forward heading (0,-1) with nothing in view; bot prop breaks 5-8 → 16/run side effect reported), tests 327/327, screenshots captures/n6-4/ | — |
| 2026-08-18 | N6-3 post-popup grace + earned recovery (QA-3 findings 1·2): 1.5s contact grace on every choice-screen close through the popup queue (effects.json popup_grace_sec, existing invuln blink telegraph, once per queue drain, CombatMath.grace_extend/grace_tick maxf rule — never stacks or carries, 4 unit tests), opening pulses left untouched (idle-probe 0-30s damage 102.0 / death 33.2s identical before-after), break-table health 6→10% (plain 84% over the 0.8 validator floor), elite-kill heal 15% max HP (pickups.json elite_heal with (0,0.5] validator contract, shared heal path with float label + green pulse, runtime-proven 50.4→69.3), Pickups.heal_budget pure pricing helper (3 tests), playtest heals/hp-at-elite/hp-at-boss/grace-shot instrumentation + pickup_check elite-heal probe, measured normal bot heals 0→6 (113.4hp, budget 161.3), hp at boss 96 flat → 122.4/150 recovered, surge fps min 60 avg 60 (baseline held), guard compensation soft_enrage.damage_mult_max 2.5→2.8 (grace made metamax-nopick unlosable at 2.5 — 12/12 survivals; at 2.8 defeats observed: nopick 293.3s, metamax nopick 260.5s), tests 317/317, screenshots captures/n6-3/ | — |
| 2026-08-18 | QA-3 second new-player experience audit: fresh-save real-windowed session (title→runs→camp→명부수 purchase→괴이록→pause) + rendered bot run for surge/boss/chest/evolution visuals (victory 274s, 1 evolution via chest, 8 prop breaks, 6 chests), 2 small fixes (per-grade pill tints via UiPalette.GRADE_* with words kept primary + grade_id on card display, disabled wood-button text to TEXT_MUTED_ON_DARK for the 엽전 부족 CTA), opening-difficulty-at-human-pace flagged UNVERIFIED (desktop contention; idle bot dies ~30s by design, perfect bot untouched — owner 30s self-check requested), 11 queue candidates + QA-2 leftover verdicts recorded in the queue row, tests 311/311 | — |
| 2026-08-17 | N4-9 earned evolution: trash special drops cut to near zero (spirit 도깨비불 0.005 only), elites the special source (0.12/kill sum, 화령석-weighted), validator-enforced rarity caps (drop_tables._config), level_required 3 milestone gate on every recipe (FTUE first run waived), 천운 luck node (+25%×2 special-only multiplier, cap 0.5), unperformed evolutions masked ??? on the card until the 괴이록 records them. Measured: 0.55 specials/run (8/20 runs), luck max 0.90/run (exactly ×1.5), meta-max 2 evolutions/10 runs, FTUE 1/1 | — |
