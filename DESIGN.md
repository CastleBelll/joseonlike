# JOSEONLIKE — Design System (SSOT, v3 full rebuild)

기준: 2026-08-14 오너 지시 — 전면 리빌드. 벤치마크는 설화(Tailbound)
실플레이 캡처 `example/KakaoTalk_*.png` 7장이며, **문법을 따르되 복제하지
않는다**. 모든 UI는 이 문서의 토큰·화면 스펙만 사용한다. raw 색값 하드코딩 금지.

캡처 대응표: `_00` 타이틀 · `_01` 수행자(캐릭터) 선택 · `_02` 명부수(메타 트리)
· `_03` 난이도 선택 팝업 · `_04`/`_06` 전투 HUD · `_05` NPC 조우 다이얼로그
· `_07` 파워 업(레벨업) 팝업.

## 1. 타이포그래피

- 전역 폰트: **Neo둥근모** (SIL OFL 1.1). 픽셀 폰트 — 안티앨리어싱 OFF.
- 크기 스케일: TITLE 28 / BODY 20 / LABEL 16. 강조는 굵기가 아니라 **색**.
- 다크 화면 제목 = GOLD. 종이 패널 제목 = 진갈(WOOD_TEXT) 또는 VERMILION.
- 본문: 다크 위 TEXT_ON_DARK, 종이 위 INK.

## 2. 팔레트 토큰

| 토큰 | 값 | 용도 (캡처 근거) |
|---|---|---|
| NIGHT | #16110d | 다크 화면 배경. 선택 화면은 사실상 검정(#0d0b09)까지 허용 |
| NIGHT_BROWN | #241a12 | 메타 화면(명부수형) 배경, 다크 카드 바탕 |
| PAPER | #ede0c4 | 종이 패널 바탕 |
| PAPER_CARD | #f6ecf0 계열 | 종이 패널 **안** 카드 (레벨업 카드가 살짝 밝음) |
| WOOD | #e2a057 | 버튼 바탕 |
| WOOD_HOVER / WOOD_PRESSED | #edb26c / #c08544 | 버튼 상태 |
| WOOD_BORDER | #6e4322 | 버튼·패널 테두리 3px + 안쪽 1px 하이라이트 베벨 |
| WOOD_TEXT | #4a2e14 | 버튼 글자 |
| INK | #1a1613 | 종이 위 텍스트, 외곽선, 아이콘 웰 배경 |
| GOLD | #ffd94a 계열(다크 위 제목) / #c49a3d(테두리) | 선택 테두리, 다크 제목 |
| VERMILION | #bf402a | 등급 pill, 낙관, 위험 |
| SUCCESS | #58d858 계열 | "선택됨" 닷+텍스트 (다크 위 밝은 그린) |
| ACCENT_PER_CHARACTER | 캐릭터별 1색 | 이름·칭호에만 사용 (예: 설아=금적, 무운=보라) |

접근성: 정보를 색 단독으로 전달하지 않는다 (pill 텍스트·닷+라벨 병행).
실픽셀 대비 WCAG AA 4.5:1 — layout audit로 강제.

## 3. 컴포넌트 문법

### 나무 버튼
- 라운드 사각 WOOD 바탕, WOOD_BORDER 3px, 안쪽 밝은 베벨 1px, corner ~12.
- 글자 WOOD_TEXT 중앙. **텍스트 전용** (버튼 안 아이콘 금지 — 예외: 광고
  유도 버튼의 ▶ 아이콘 1개, 캡처 `_05`).
- 폭: 프라이머리 전폭~85%, 높이 64+. 스택 간격 24~32.
- disabled 없음 — 조건 미충족이면 **숨긴다**.

### 종이 패널 (팝업)
- PAPER 바탕 + WOOD_BORDER 3px, corner 12. 헤더 밴드(제목 중앙) + 본문 영역
  구분. 4모서리 **창살(lattice) 장식** — 은은한 회갈색 라인 패턴.
- 팝업 중 시간 정지. 화면 하단 바깥 또는 패널 하단에 CTA 나무 버튼.

### 선택 카드 (가로 행 카드 — 캐릭터/난이도/레벨업 공통)
- **가로 전폭 행 카드가 세로로 스택**된다 (캡처 `_01`, `_03`, `_07`). 3열
  그리드 금지.
- 좌측: 정사각 아이콘/초상 웰 (다크 배경, corner 8). 웰 아래 보조 라벨
  (Lv.N / 신규! / 잠김).
- 중앙: 이름(크게) → 칭호/부제(액센트색) → 설명/대사(본문색) 순 세로.
- 우측 상단: 등급 pill(VERMILION 바탕 흰 글자) 또는 상태 표시.
- 선택됨 = GOLD 3~4px 테두리 + SUCCESS 닷 + "선택됨" 텍스트.
  비선택 = 어두운 저채도 테두리 1~2px.
- 체크형(난이도)은 우측 원형 체크 배지.

### 전투 HUD (미니멀, 캡처 `_04`/`_06`)
- 상단 좌: ‖ 일시정지 + ⓘ 정보. 상단 중앙: **대형 픽셀 타이머** (흰 글자
  + INK 외곽선, 배경 칩 없음).
- 그 아래 **전폭 얇은 XP바**: 트랙 다크브라운, 채움 WOOD 오렌지.
- 바 아래 좌 "Lv.N" 소형 흰 글자. 우측: 해골 아이콘+처치수, 엽전 아이콘+수.
  아이콘+숫자만, 칩/패널 배경 금지.
- 우측 중단: 원형 이벤트/보급 버튼 1개 (진행 링 게이지 포함) — 해당 기능
  있을 때만.
- HP는 최소 표시. 화면을 UI로 덮지 않는다.

### NPC/내레이션 다이얼로그 (캡처 `_05`)
- 하단 고정 다크 패널(NIGHT_BROWN + GOLD 계열 테두리): NPC 이름(GOLD) +
  구분선 + 본문(흰) + 보조 정보 1줄(SUCCESS) + 나무 버튼 세로 스택.
- 화면에는 NPC의 확대 스프라이트가 등장한다 (인월드 소형 스프라이트의
  확대 연출).

## 4. 화면 스펙

### 타이틀 (캡처 `_00`)
- 풀블리드 야경: 상단 별하늘+달, 하단 한옥 마을 실루엣 + 점등 등불.
- 로고: 기와지붕 간판(현판) + 좌우 등롱 + 붉은 낙관 도장, 상단 ~12-30% 영역.
- 좌상단 코너 유틸 아이콘 열(설정 등 48px). 우상단 재화 표시(해당 시스템
  생기면).
- 하단 ~25% 영역에 나무 버튼: **[게임 시작] 단 하나** (오너 지시, N5-2 —
  이어하기/종료 버튼 없음, 자동 저장·자동 재개). 버튼이 마을 실루엣 위에
  얹힌다. 수행자 선택 등 부가 화면 진입은 별도 동선으로 추가 예정.

### 수행자 선택 (캡처 `_01`)
- 배경 근검정. 상단 GOLD 제목 "수행자 선택".
- 캐릭터 가로 행 카드 스택 (§3 선택 카드). 이름은 캐릭터 액센트색 +
  (한자 병기), 칭호 1줄, 대사 1줄("따옴표").
- 잠긴 캐릭터: 실루엣 초상 + 해금 조건 텍스트. 숨기지 않는다 (콘텐츠 예고).
- 하단 나무 버튼 [나가기]. 선택 즉시 반영(선택됨 배지 이동).

### 파워 업 / 레벨업 (캡처 `_07`)
- 종이 패널 + 창살 코너, 제목 "파워 업!". 선택지 3장 가로 행 카드 스택.
- 카드: 아이콘 웰(다크) + 웰 아래 Lv.N/신규! + 이름 + 효과 설명(실수치) +
  등급 pill.
- 패널 밖 좌하단: 보유 무기 아이콘 행 (각 아이콘 아래 Lv.N).

### 난이도/지역 선택 팝업 (캡처 `_03`)
- 종이 패널 + 창살 코너 + 제목. 체크형 행 카드 리스트(스크롤), 선택 카드는
  주황 테두리+체크 배지. 배수/모디파이어는 텍스트로 명기.
- 패널 하단 CTA [도전하기], 패널 밖 하단 보조 버튼.

### 메타 진행 — 명부수형 (캡처 `_02`, 후순위 페이즈)
- NIGHT_BROWN 배경, 좌상 ‹뒤로+제목, 우상 재화 pill. 탭 2개(pill형).
- 중앙: 신목(나무) 위 노드 그래프 — 노드=원형 아이콘+자물쇠+이름+비용.
- 하단: 선택 노드 상세 카드 + 전폭 CTA.

## 5. 인게임 월드 룩 (캡처 `_04`/`_06`)

- 탑다운-라이트 시점(위에서 비스듬), 다크 포레스트 팔레트 — 저채도 진초록
  지형에 더 어두운 명암 패치, 나무/바위 프롭. 화면 전체가 어둡고 UI가 밝다.
- 플레이어: **2등신 청키 픽셀**, 인월드 논리 크기 ~28-32px (540 기준),
  1px 다크 외곽선, 플랫 셀 셰이딩, 캐릭터당 액센트 1색. 좌/우 2방향 +
  4프레임 걷기.

### 5.1 캐릭터 아트 계약 (오너 락, 위반 시 리테이크)

**레퍼런스는 `new_asset/`의 오너 제공 PNG가 1순위다** (`basic.png`,
`Character.png`, UUID 파일 4종). 설화 캡처 `_01`의 설아/무운 카드 초상이
2순위. 이 둘을 보지 않고 생성 금지.

**도사는 오너가 직접 확정했다**: `new_asset/taoist.png`(전신+초상),
`new_asset/taoist_walk.png`(8프레임 걷기). 그린스크린 배경. 이 시트가
도사의 최종본이며 재생성 대상이 아니다 — 크로마키·정렬·다운스케일만
한다. 이후 캐릭터도 같은 방식(오너 시트 → 가공)을 우선한다.

- **나이: 젊다.** 10대 후반~20대. 수염·주름·노인 실루엣 **금지**.
  "늙은 도사/산신령/아저씨" 방향은 전부 리테이크 대상이다.
- **얼굴: 애니풍 미형.** 큰 눈(흰자+또렷한 홍채), 작은 코·입, 볼 홍조,
  또렷한 검은 아웃라인. 사실적 얼굴·거친 얼굴 금지.
- **머리: 화려한 채색.** 진분홍/진홍/청람 등 고채도 헤어가 실루엣의
  주요 식별 요소다 (레퍼런스 전부 그렇다). 갈색·회색 무채 헤어 지양.
- **비율: 2등신.** 머리가 전체 높이의 약 절반, 짧은 몸통·짧은 다리,
  손발은 뭉툭한 블록.
- **채도: 높다.** 플랫한 고채도 색면 + 1~2단 음영. 세피아·탁한 팔레트 금지.
- **성별 균형**: 로스터는 미형 남녀 혼합. 설화의 무녀(설아) 같은
  "이쁜 여성 픽셀 캐릭터"가 기준선이며, 남성 캐릭터도 같은 미형 기준.
- 정체성 소품(도사=삿갓·석장·부적)은 **이 룩 위에** 올린다. 소품이
  캐릭터를 늙게 만들면 소품 쪽을 바꾼다.
- 몬스터: 같은 픽셀 밀도, 실루엣 대비(백골=흰색, 도깨비=녹색처럼 명확한
  팔레트 분리). 데미지 숫자: 소형 흰 픽셀 숫자, 크리티컬만 금색.
- XP 구슬: 시안-그린 발광 소형 오브.

## 6. 금지

- 3열 그리드 선택지, 버튼 안 아이콘(광고 ▶ 예외), disabled 회색 칠(숨김으로
  대체), 빨강 대면적 버튼, 종이 위 종이 중첩, raw 색값, 색-단독 정보 전달,
  화면을 덮는 HUD 칩/패널.
